[package]
name = "jury-protocol"
description = "Versioned public protocol boundary for Jury witnesses"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
base64 = "=0.22.1"
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.145"
sha2 = "=0.11.0"

[dev-dependencies]
hex = "=0.4.3"

[lints]
workspace = true
