[package]
name = "jury-core"
description = "Domain boundary for the Jury secrets vault"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
proptest = "1.7"
serde_json = "1.0"

[lints]
workspace = true
