[package]
name = "darwin-process-info"
version = "0.1.0"
edition = "2024"
rust-version = "1.90"
license = "MIT"
publish = false
description = "Maintained checked Darwin process information provider for Jury"

[target.'cfg(target_os = "macos")'.build-dependencies]
bindgen = { version = "=0.72.1", default-features = false, features = ["runtime"] }

[workspace]
