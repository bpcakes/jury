Consumer: J14 exec/run pipeline. Gated feature: Jury-owned child-process containment with bounded redacted capture. Defect class: descendant leaks, PID/PGID reuse races, unbounded pipes, lost signal status, and partial-spawn cleanup. Implement a neutral jury-process crate from the verified Jig baseline while preserving unsafe_code=forbid through safe maintained syscall providers; validate containment, cancellation/timeout, forwarding/status, bounds, redaction, spawn/setup failures, platform refusals, and no Jig Cargo dependency on native Linux. At the operator's 2026-08-30 request, macOS implementation and validation moved out of active 0.x scope. The provisional macOS backend is retained for deferred task jury-qv4.3.4 but contributes no J12, J14, CI, packaging, or release evidence. Deletion condition: close this J12 work record only after its Linux-only acceptance and repository gates pass; jury-qv4.3.4 remains deferred until an explicit post-0.x macOS scope is approved.
