Consumer: the maintainer releasing Linux 0.0.1. Gates witnessed request persistence, valid acknowledgement inspection, and accurate default-field redaction UX after the defects reproduced in docs/qa-linux-0.0.1.md. Work separately: repair bounded replay persistence with legacy-empty-state compatibility and real SQLite/daemon regression coverage; repair bare/wrapped acknowledgement parsing without weakening signature checks; make the documented field/output contract explicit and test it. Run Claude plus Codex comprehensive review for each change, up to two rounds, address actionable findings, then run combined native lifecycle and required Jig checks. Delete or supersede this plan when all three fixes and their validation are integrated.
QA-01 review round 1: Claude and Codex completed against fingerprint d18a1324a8236dd3ba935aa4932562987b36bff25844a701748418501f3b5378 (complete, unchanged; Claude restricted). Codex found no actionable issues. Claude identified CI timeout/diagnostic/cleanup issues; switched CI to release binaries, added bounded diagnostics that withhold known fixture material, aggregate cleanup failures, and simultaneous ephemeral port selection. Clarified the capacity paragraph. The release lifecycle passed again after these changes.

Adjudication: retained whole-snapshot storage is capped at 64 MiB, with explicit operator compaction under the existing protocol. Incremental storage and automatic retention changes are separate architecture work, not needed to repair nonempty serialization. A new representation marker cannot make an already released old binary emit a new error: the existing version check itself returns InvalidState. Empty old snapshots remain byte-compatible, and downgrade limitations are explicitly documented. Key/request disagreement is an internal invalid replacement, already rejected before commit; the existing store trait has unavailable/capacity errors only. Expanding that protocol-facing trait is not necessary for this fix. Simultaneous test port reservation prevents duplicate selections but cannot eliminate the normal release-to-bind race; this is a residual harness limitation. These are parent adjudications, not additional independent reviewer findings. Actual debug/release lifecycle runs and warning-denying clippy answered the empty-CA and serde-warning questions. The lifecycle gate intentionally runs in Linux CI and is directly runnable locally.

QA-01 round 2: Claude and Codex completed against 7ccb5adad2198ce95c8fd3456ffa1c360a8d58980934dce3af3626f53242e49f, complete and unchanged, Claude restricted. Codex found no actionable findings and again passed storage tests plus the real lifecycle. Claude repeated the storage-cap, downgrade-marker, key-invariant and port-race concerns; the first three retain the round-1 adjudications. Its claim of an unrecoverable cap outage is overstated: expired records can be compacted; unexpired records must remain retained. Added explicit deployment guidance about that availability window and the byte cap preceding the protocol count bound. No throughput benchmark is claimed. Its suggestion to revert CI to dev contradicts round 1's timeout finding without runtime measurements, so retain the tested release build. identity-init JSON has no filesystem path field, so its conditional suggestion to consume one is inapplicable. Fixed the remaining diagnostic gap using the existing private-material filter on traceback output. These two follow-up changes are locally verified after the second/final review; no third review is claimed. Residual tests do not prove crash recovery during anchor publication or saturation throughput.

QA-02 round 1: Claude and Codex completed against 14aedc02972fc14ae240aaadeba9ffc238b991d6584bd021cb45f38dbd09eda6, complete/unchanged, Claude restricted. Both confirmed the parser repair. Fixed Claude's Linux test gating and fixture-wording findings, covered all four bare/wrapped combinations, and asserted phase/checkpoint digest. Retained the pre-existing authentication error contract for malformed input; a new error taxonomy and sharing transport envelopes through the canonical protocol are separate API changes. Unsigned envelope metadata remains intentionally ignored; signed checkpoint/anchor verification is authoritative. Round 2: Codex completed cleanly against 6295a97f7703eca0a73645c48a000d8968ab36798aa707d1cbbcaa1e8815ce71 and passed the real CLI regression. Claude's adapter exited 1 without a report; no third attempt. Final scope complete/unchanged. The release CLI now reports durably-accepted 2/2 for the original audit artifacts; tampered signatures remain refused in regression coverage.

QA-03 round 1: native Codex completed against 1336d69dbee20bbf0362a56fdd79af6c4563782fe84a467edc63889210a23743, complete/unchanged. Its low-severity README finding (README.md:218; source: Codex) was correct: the implementation filters common encodings as well as raw values. Corrected that claim. Also documented and diagnosed the existing four-byte concealed minimum, added short-value/boundary regressions, and made the two existing plaintext-output fixtures explicitly unconcealed without weakening their assertions. Both Claude adapter attempts exited 1 without reports; they were not retried again or counted as completed reviews. Round 2 native Codex completed with no actionable findings against b68cb96801eae06c2d7336d409f8a5dc293a26d7b36744d8d0becd2dd24ac082, complete/unchanged, and passed the new field regression, parser check, and existing main CLI journey. All final QA-02 and QA-03 files match their final reviewed copies.

Review notes: QA-01 Claude/Codex completed both rounds; QA-02 Claude/Codex completed round 1 and Codex completed round 2 while Claude failed; QA-03 Codex completed both rounds and Claude failed both attempts. Claude file access was restricted throughout. Completed scope fingerprints were verified. These are model reviews, not independent security review. Open questions: none blocking the three repairs. Residual test gaps: crash during anchor publication, saturation throughput, and rejection/readback of a too-short replacement to an existing concealed field (short creation, explicit text creation/update, and the four-byte boundary are covered). No implementation defect was established for that last optional gap.

Final validation: targeted replay storage tests, acknowledgement CLI regression, field-default/parser regressions, release builds, full workspace clippy, formatting, Rust file-size policy, and Jig contract all passed. The release Linux lifecycle passed exact witnessed read, receipt verification, stable replay across restart, and persisted cancellation. An additional real-daemon run verified newly generated registration responses through the release CLI as durably-accepted 2/2. `scripts/jig check test` passed (run_01M1SQE6BR27HBMP98TXC8E0VX, exit 0); `scripts/jig work check` passed all required legacy gates and the verify profile (run_01M1SR4M3S7QNQHY0J2SQPNZ1Y, all five targets successful). Source stayed unchanged during the checks. Jig's configured legacy/profile passes duplicate the workspace suite; no test, gate, assertion, or timeout was weakened to obtain these results.

The three requested repairs are complete. Package versions, release packaging, other audit observations, and external security review were not included in this repair scope. No application commit, push, or release was made.

User-directed cutover supersedes the earlier compatibility decision: no released vaults or databases exist. Removed the legacy empty replay-object decoder and serializer branch; empty and populated snapshots both use record lists, and old object snapshots are refused. Updated the regression to assert the new empty representation and explicit old-format rejection. This is a post-review scope correction after both QA-01 rounds; local storage and real daemon lifecycle verification are required, with no third comprehensive-review claim.
