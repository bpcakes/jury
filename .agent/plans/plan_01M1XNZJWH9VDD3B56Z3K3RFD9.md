Consumer: the required CI file-budget gate. Resolve the two debt-growth failures in 16280cc by placing passphrase tests in their existing module and isolating cohesive J25 timing sampling/statistics. Preserve measurement entrypoint, JSON fields, threshold, warmups, sample counts and test assertions. Validate against the review parent, not the advanced branch tip; run affected Linux tests and standard checks. Retain this plan only as required Jig work evidence for this fix.

Implementation: the existing native_cli/passphrase_input.rs module now owns the strict-capture and non-terminal opt-in tests, preserving their bodies byte-for-byte. J25's timing.rs owns the unchanged paired warmup/sampling loop, statistics, report shape and gross-divergence threshold; measured operations and the release_measurement_case entrypoint remain in their original module. Zero-variance assertions moved with the statistics code, and a known-sample test checks sample variance and class-order symmetry. No policy ceilings, waivers or exemptions changed. J25 parent shrank from 1706 to 1610 lines; additional.rs from 913 to 847. Existing size debt shrinks under the unchanged no-growth policy.

Verification so far: direct native diagnostics and repo:file-budget both pass with explicit comparison 9323a5562e11be5e7af86ea202d2fc038561e516. Format, core Clippy with all features/targets and warnings denied, and three J25 parser/statistics unit tests pass on macOS. Standard scripts/jig check test still cannot compile the CLI on macOS because of existing rustix memfd imports. Linux workspace tests are running from a read-only bind of this working tree; both relocated tests have passed. Jig 0.3.0 rejected direct native check --plan-id due to a run-plan/work-plan mismatch, so the actual target was verified without that option; forced work check executed contract and format successfully, but the older-baseline scope error prevented those results from satisfying the gates.


Final validation: complete Linux ARM64 workspace test/doctest run passed 461 tests, 0 failed, 2 existing ignored using Docker --init, a read-only source mount, cached dependencies with --offline/--locked, and unlimited memlock. Initial run without --init had 19/21 native CLI tests pass and failed the process-liveness probe plus a transfer BrokenPipe; the corrected container run passed all 21 with identical assertions and source. Both relocated tests and all three J25 parser/statistics tests pass. This does not constitute a fresh release-mode timing measurement or independent security review. Bead jury-nde is closed for the completed fix.

The final original-baseline native budget check passes. Jig work evidence/gates/finish were inspected; the plan remains open because complete fresh native work-profile receipts are not available and the macOS workspace compile limitation remains. No work gate or receipt was fabricated to close it. The operator subsequently authorized committing these changes together with the upstream reconciliation evidence.
