Consumer: scripts/new-checkout.sh. Gated feature: creating a sibling checkout before copying local environment secrets. Observed defect class: bootstrap can regenerate reviewed managed helper or workflow boundaries and return an unsafe checkout. Control: compare reviewed boundary files after bootstrap and re-run immutable action validation before publishing .env. Delete this plan record under normal Jig retention after the session closes; the runtime guard may be removed only when the pinned upstream templates enforce equivalent invariants and regression coverage proves it redundant.