Implement the bounded signed transfer envelope, public and identity-scoped inspection, export freshness status, and dry-run/atomic import. Existing homes accept only byte-identical validated state or a complete authenticated strict descendant; every divergence is a typed no-write conflict. Add focused adversarial and CLI integration tests, then run affected gates and the required backend test gate.