Consumer: maintainer implementing the four user-requested duplicate-abstraction fixes. Gates this consolidation, not a release. Observed defect class: duplicated checkpoint checks, worker lifecycle loops, operation mapping, and output-path normalization can drift. Preserve typed errors, public boundaries, signed bytes and durable formats. Implement one cluster at a time with behavior tests; run formatting, Clippy, test and required Jig gates. Close when all four extractions and checks pass. Retain only required append-only Jig recovery history after closure.

Progress: DU-001 through DU-004 are implemented. Checkpoint validation lives in jury-core/src/checkpoint_validation.rs with consumer-specific error mapping. Witness and anchor workers share jury-witness/src/state_worker.rs. The request creator uses policy::core_operation. Read and template output paths share jury/src/cli/output_path.rs. Five new tests cover checkpoint failures, worker lifecycle, and path normalization. No public or durable format changed.

Validation: direct and witnessed input gates passed. Focused tests passed. The full workspace test gate, Clippy, formatting, contract, and Rust size gates passed. The default work check and its separately configured verify profile both passed; all six gates have fresh evidence for the final source. DU-005 through DU-007 remain intentionally separate. All implementation and verification work is complete. No commit or push is requested.
