Consumer: maintainers using the uncommitted macOS work. Reconcile upstream CLI/release/fixture changes without losing local capability or either append-only history. The observed overlap is moved approval publication code, two closed-input fixes, and CLI scope text. Baseline e3996aa plus restored local work; recovery stash 0470a8cfb76fff5c34aaaa71dcb6744595811fe8. Validate native and Linux CLI/workspace checks and unchanged upstream behavior. No commits or pushes. Delete/supersede this plan after reconciliation and verification; remove the recovery stash only after preservation is verified.

Reconciliation: main fast-forwarded across 21 upstream commits to e3996aaf032135df5fbd60776b444f94739b362f. Seven conflicts were resolved without committing local work. Upstream onboarding and banner removal remain, alongside truthful native macOS scope help. The upstream isolated real-pipe regression supersedes the local socket-based variant; local input delivery checks and macOS root overrides remain. Atomic approval publication was transplanted into the extracted engine fixture. Renderer copying now respects the upstream JURY_TEST_BINARY override. Both append-only history streams are retained, and tracker records were merged by ID/field and imported through br. Additional existing database metadata/comments were preserved on export. Development diagnostics are now explicitly distinguished from published v0.0.1.

Verification: native CLI suite passed (24 shared integration cases and five native witnessed cases, plus unit/default-root tests). Full native workspace testing exposed the newly upstream configuration fixture using an aliased temporary SQLite path; canonicalizing the owned root repairs it without changing production path checks or assertions. Both configuration regressions and final scripts/jig check test pass. Linux Docker validation could not start: the test container and a trivial no-network/no-mount probe remained Created. Task-owned clients were cancelled and container cleanup requested; no Docker restart or unrelated-container changes were made. Required gate refresh and final preservation check follow. The recovery stash is retained until verification and cleanup finish.


Outcome: full native workspace tests, formatting, all-feature Clippy, contract and file-budget gates pass with fresh evidence. HEAD equals origin/main at e3996aa, git diff --check passes and no conflicts remain. Both original history streams are present verbatim. Local work remains uncommitted. Linux execution remains unverified because Docker startup stalled independently of mounts/network; the probe was removed, while removal of the non-running jury-reconcile-linux container remains pending. Keep recovery stash 0470a8cfb76fff5c34aaaa71dcb6744595811fe8 until the operator no longer needs the pre-reconciliation snapshot; existing older stashes are untouched.
