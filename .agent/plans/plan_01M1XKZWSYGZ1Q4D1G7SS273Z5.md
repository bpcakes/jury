Consumer: Jury CI and local verification. Replace the temporary path-filtering LOC wrapper with the released native file-budget action; preserve the authorized provider-only size exclusion and existing application limits. Validate exclusion and sibling/application rejection in an isolated Git fixture, verify contract/CI wiring, and report existing unrelated build or size debt honestly. Baseline snapshot is retained temporarily outside the repository to recover customized managed files during update; remove it after verification.

Upgrade implemented: template source is v0.3.0 (8629700b92cd9ab8b09f8ff86de4fc1573469c83), contract v7, native repo:file-budget action and target evidence gate. The repository-owned policy keeps the 800-line ceiling and excludes only third_party/sanitization/**. Both old wrapper files are removed. CI passes explicit event comparison authority. Custom Linux integration steps, pinned actions, agent map, and hardened new-checkout script are preserved; the old no-mod-rs workflow uses a local tracked-filename check because the Jig subcommand was removed.

Validation: nine isolated native checks passed: provider-only direct/action staged checks; application and similarly named sibling violations rejected through direct/action checks; invalid baseline blocked; explicit and push-before CI comparisons passed. The preserved no-mod-rs workflow body passed valid-file and rejected tracked-mod.rs fixtures. Contract, format, agent-map and action-pin checks pass. The actual native budget check reports existing application debt growth in j25_measurements.rs (3 lines) and native_cli/additional.rs (42 lines). Full macOS test and Clippy checks retain the pre-existing Linux-only rustix memfd import failure. Native validate/audit inventory is additionally blocked by existing license symlinks and the unstaged legacy-script deletion; changed-file checks execute and classify the provider correctly. No exemptions or ceilings were added for these unrelated failures.

Final work evidence/gates/finish were inspected. Finish remains blocked by the existing size/build failures and partial staging of .beads/issues.jsonl (legacy gate attestation is unknown). The user index is unchanged. Bead jury-skl is closed for the completed upgrade; this work plan remains open rather than claiming repository-wide green gates. Temporary pre-update recovery snapshot removed after verifying preservation of all unrelated application/provider files, custom CI/checkout scripts, and append-only state journals.
