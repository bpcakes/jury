Consumer: M01 maintainers and native acceptance tests. Feature: trustworthy isolated protected-memory tests. Observed defects: successful zero-test child can satisfy isolation helper; Unix-only test calls break Windows test compilation. Replace duplicated test-name and boolean dispatch with a named test wrapper and child completion acknowledgement; separate native Unix operations from pure tests; prove missing-test, failed-child and timeout rejection; check Windows test compilation and macOS/Linux native suites. Review with selected Codex, Claude Opus and Cursor passes and fix actionable findings until clean. J25 Darwin adaptation stays M08/M09; pinned provider emergency report policy is correct. Supersede this plan when fixes, verification and review loop complete. Preserve all existing workspace edits; no production memory contract or provider pin change is planned.

Research and implementation (2026-09-05): the pinned provider retains the requested Exclude policy when preferred fork establishment fails; only its state becomes Failed. No provider contract change is required. J25's Linux-only runner is intentional; Darwin adaptation remains M08/M09. The hosted transfer snapshot failure has no attributable path in the truncated logs; a Git writer remains an unproven hypothesis. A temporary validation-checkout diagnostic reports changed paths without file contents.

The isolated-test macro derives the libtest name and owns dispatch. Parent success requires child success plus a per-invocation completion file written only after the body succeeds. Child cleanup is guarded and the timeout path waits for reaping. Native core operations and fault hooks are cfg(unix); portable decision tests use an explicitly synthetic status.

Local verification: macOS 26.6.2 arm64/Rust 1.98.1 protected-memory suite 41 passed, zero failed/ignored; targeted Clippy passed. Windows GNU test compilation passed (runtime not executed). Linux arm64 Debian 12/Rust 1.97 scripts/jig check test passed, including transfer inspection; CARGO_PROFILE_TEST_OPT_LEVEL=1, assertions and selection unchanged. Hosted native matrix and selected reviewer loop remain pending. Claude Opus reported a session quota limit resetting at 21:50 Europe/Prague; that failure does not count as a clean review.

Hosted run 33985585475 at validation commit 56ff4dce793a339567575a0aea60e0e804f95a91 passed all lanes: macOS 15 arm64/x86_64 (Rust 1.90), macOS 26 arm64 (Rust 1.98.1), Ubuntu 24.04 x86_64 (Rust 1.90). Provider: 112 on each Mac, 164 Linux; Jury protected-memory: 41 each Mac, 40 Linux, all zero failed/ignored. Full hosted Linux fmt, Clippy, contract and tests passed, including the unchanged transfer behavior with a temporary bounded failure diagnostic. No transfer failure reproduced, so no causal claim or speculative fix is made.

Review round one used verified unchanged fingerprint a5b6ee538f9b6dda0dcd307d09f42bfebb3dcbd2651f9e23a6f946ccde5828cf. Codex found no actionable defects. Cursor's fork-policy concern is disproved by the exact pinned provider report contract and native preferred-failure test; this contract is now documented beside the check. Cursor also identified avoidable process-wide MEMLOCK mutation in the TUI rendering test. The rendering decision now has a pure private message function tested with explicit degraded/established inputs; no allocator or resource-limit mutation is needed and the test-only rlimit dependency is removed. Native status semantics remain covered in jury-protected. Cursor coverage was limited by its 64 KiB untracked-file cap omitting docs/macos-support-plan.md (111392 bytes); 40/40 captured pages were reviewer-attested. Claude Opus exited before producing a report due the diagnosed session quota. A fresh review round is required for the TUI repair.

After the TUI refinement, native macOS TUI tests (3) and Clippy passed, and Linux work check passed all configured checks plus the verify profile, including full workspace tests. Cargo Nextest 0.9.130 on native macOS ran all 41 protected-memory cases successfully (zero skipped), resolving the runner compatibility question for that tested version. Hosted final validation at 8e64bde22e162a6232b86d45d3d57a896c634742 is running as 33986558491; the temporary transfer diagnostic has been removed.

Review round two completed on unchanged fingerprint 72ee345835b15d8050a45ca898a9b71fec75799de3d92186a9c02f239ce6e224. Claude Opus completed this time (43/43 captured pages reviewer-attested; large macOS plan omitted by external cap), as did Codex and Cursor. Accepted follow-ups: validate a fresh per-invocation pending completion record before child dispatch; restrict BrokenPipe tolerance to typed CLI rejection exit 2 and test real closed pipes for exits 0/1/2/101; restore the opt-in test's small input. The closed-pipe test retains ChildStdin separately while waiting, since Child::wait closes its stored stdin. Added explicit minimal MEMLOCK precondition diagnosis and documented provider accounting semantics. No skips or assertion weakening.

Adjudication: J25's current runner explicitly requires Linux and the measured standalone allocation contract does not require process core suppression; replacing its predicate with is_degraded would add that requirement, so its Darwin adaptation stays M08/M09. M01 explicitly requires the shared predicate in secret_input and auditing other consumers, not porting the J25 runner. The timeout regression intentionally expects Timeout; slow startup cannot cause the claimed false failure. TUI's public wrapper is a thin delegation to the tested message decision and the protected status predicate; adding a synthetic public status constructor solely for wiring coverage would weaken the owning boundary. The provider contract assertion is now explicitly named as such. The 8-to-16 MiB doc correction matches the pre-existing constant and M01's explicit stale-doc instruction; protocol limits are unchanged. Linux standalone status may remain degraded before capture by design. Future provider behavior changes are not present defects in an immutable pin.

Hosted run 33986558491 at 8e64bde22e162a6232b86d45d3d57a896c634742 passed every lane, including all native TUI cases and full Linux checks with the original transfer assertion restored. The original transfer failure remains unattributed and did not recur. New dispatch changes passed 42 Nextest cases on macOS and Windows test compilation; an inherited-case-only native subprocess probe passed. Next verification/review round remains pending for the accepted follow-ups.

The real closed-pipe regression passed on Linux, followed by every configured work check and verify target (fmt, Clippy, contract, LOC, full workspace tests). Final candidate validation is running at 28b06b7b617cca55f224307c58e10ee221496a91 as hosted run 33987699382. The protected-memory Nextest run included the explicit MEMLOCK precondition and passed 42 cases, zero skipped.

Review round three completed on verified unchanged fingerprint 7ae742369603b0170f1116df668284606cac03f0c2f9a7d95a229ef9b2678164; all three requested reviewers completed. Codex found no actionable defect. Cursor's /bin/sh portability concern is disproved by native_cli.rs:1 gating the entire target to Linux. Opus's large-input and Deserialize claims describe absent code: the fixture is small again, and ProtectionStatus derives Serialize only. Restoring satisfies(request) would reject the deliberately Unsupported preferred Darwin dump control; accepting hypothetical future Established reports would abandon the explicit frozen M01 contract. The immutable provider cannot produce that report on Darwin, even if the OS is upgraded; a provider revision requires a fresh integration review. Public-constructor documentation of irreversible Darwin core suppression was actionable and is now added, together with the existing native-only harness and Linux-only J25 distinctions. TUI wiring is a thin delegation, not a current defect.

Completing the accepted invocation-protocol fix exposed an ambiguity: invalid complete child context must fail, not be mistaken for parent context and recursively spawn on a pending-record read failure. matching_completion now distinguishes incomplete parent markers from InvalidInvocation. Its regression checks absent, mismatched, valid pending and stale completed records. A real native subprocess probe confirmed incomplete-marker recovery and immediate rejection of missing/stale complete records. All 42 Nextest cases, protected-memory Clippy and Windows test compilation passed. No product protection contract changed.

Hosted run 33987699382 at 28b06b7b617cca55f224307c58e10ee221496a91 passed all four lanes and full Linux checks. One final verification/review round covers the invocation error distinction and public rustdoc additions.

Review round four completed with unchanged complete fingerprint 9a275ef89cd880cafd431afea055ed961142c294db269d42ea09fbaa1fd66212. Codex and Cursor found no actionable defects; all three reports completed. External reports read 48/48 captured pages but omitted the oversized macOS plan. Opus identified the remaining broad BrokenPipe acceptance as a plausible loss of test signal. The helper now propagates every stdin write failure after reaping, regardless of exit code. The opt-in test supplies an empty non-terminal pipe and still requires the exact opt-in-required error before any read; no expected passphrase delivery needs an exception. The real closed-pipe regression now rejects exits 0, 1, 2 and 101 alike.

Research of RustSec Query::matches confirmed source kind and URL matching, so protected-primitives now documents that registry advisories do not automatically cover the Git fork and names M10 packaging/source availability ownership. Registry publication was already disabled. Accounting consistency and exact Darwin Unsupported are intentional contracts of the immutable provider, not evidence of a present runtime defect; both are already documented. Emergency preferred failure is tested in the pinned provider. The 1.90 native lanes actually compiled and ran the ExitCode comparison, rather than inferring compatibility. No API widening or hypothetical provider report injection was added to satisfy speculative coverage suggestions. Hosted run 33988618683 at a4fadecc55c37736107a3ad6c8707168a318e1e9 passed all four lanes and full Linux checks; final tightening of the Linux test helper and advisory documentation requires another review pass.

Round five completed on verified unchanged fingerprint 121040a78dfcddd19ed357bb883a0d52384db7f05c7a9b3bd39b5331bc918c09; Codex and Cursor reported no actionable findings. Opus again raised future provider/report/platform changes and the already authorized documented Git-source tradeoff; no present runtime defect was established. Its populated non-terminal input coverage concern is addressed with a real anonymous file containing the generic passphrase, passed through a cloned File handle: the shared cursor must remain zero after the exact opt-in-required error. This supplements the empty pipe case and removes dependence on parent write timing. Rust File::try_clone documents the shared cursor. The earlier generic exit-2 BrokenPipe acceptance was tolerance widening; it has been removed entirely.

Hosted run 33989424660 at a2329e0d425f2ab1d3092a45e1ffb330b51e0566 passed all Mac lanes but the prior transfer snapshot failure recurred at transfer.rs:515 in full Linux checks. It was not a BrokenPipe failure. Jig truncated the assertion byte dump before its useful difference, so the same exact equality assertion now reports differing paths only. No paths are ignored and no assertion is weakened. A separate 100-iteration local Git 2.55 commit/snapshot probe found no drift; background maintenance is still an unproven hypothesis. Hosted diagnostic candidate 8306548f850f2f71818c7a727b50b8a565fabd2a / run 33990226102 repeats the transfer case before full checks. Latest local work check passed all gates including full tests with the populated-input assertion and snapshot diagnostic.

Round six completed on verified unchanged fingerprint ce8364e402400a5683a050c207bcd3d814fe2bf321b2d3a4b621158f9ea873a1. All selected reviewers completed; external coverage remains limited to captured pages (53/53 receipts; large macOS plan omitted). Codex found no actionable defects. Claude and Cursor both identified missing consumer coverage for the newly shared status predicate, so a native strict passphrase capture now asserts established protection and exact bytes. It passed alone in a fresh Linux container with inherited CORE soft/hard 1048576, exercising capture from initially enabled cores. The hosted lane also runs it alone after setting a nonzero soft limit, before the complete secret-input subset. TUI remains a pure decision test plus a one-call public delegation; exposing mutable/synthetic production status solely to test that trivial delegation would broaden the API without covering new behavior. Future provider reports, source availability, and Linux-only J25 remain explicit documented boundaries, not present production defects.

Hosted run 33990226102 at 8306548f850f2f71818c7a727b50b8a565fabd2a passed all four native lanes, five transfer-case repetitions, and full Linux gates. The improved equality diagnostic remains; no transfer drift reproduced. A reduced real Jury inspect probe using a locally built Linux Git 2.55 completed 200 fixture iterations with zero drift. These negative results do not establish a cause or fix for the intermittent hosted snapshot failure. Final candidate 9ecc8c87e45b63d9a9da974084c0a0e43c7adb88 / run 33990997365 contains the added strict-capture assertion and restores the normal full-suite workflow after the bounded diagnostic repetition.

Round seven completed on verified unchanged fingerprint 3e35bb54a9a2442df2283f7ae6bc1e828e068cf34d7002bb60b6136f58c07429. Codex and Cursor reported no actionable findings. Opus identified a real presentation defect: degraded control state was conflated with selected emergency policy. The TUI now renders those two facts separately and tests all four policy/outcome combinations without native allocation or process-limit mutation. Strict degradation no longer claims an override; established emergency controls still disclose the enabled override. The new strict capture regression now runs through a real CLI subprocess, with no emergency flag and an explicit JSON protection_degraded=false assertion; the temporary library test was removed so it does not add another irreversible parent-process mutation. This CLI test passed in a Linux container with initial CORE soft/hard 1048576. All five native Mac TUI tests passed. Native MEMLOCK prerequisites are now stated in the owning crate guide.

Hosted run 33990997365 at 9ecc8c87e45b63d9a9da974084c0a0e43c7adb88 passed every native lane and full Linux checks, including the then-library capture test alone after ulimit -c 1024. The next run covers the final CLI-child version and corrected TUI policy wording. The missing permanent Mac gate is the explicitly deferred M09 scope, not absent native verification; the authorized immutable provider source and advisory limitations remain documented rather than represented as security certification.

Final review (round eight): Claude Opus, native Codex and Cursor completed on verified unchanged complete fingerprint 6f951a175b93d78da0ce8bf964bce43adf80feca815ad87d272cc16642bbd3fa. No actionable findings remain in the final patch after adjudication. Claude retained the authorized/documented Git-source availability and advisory tradeoffs, pre-existing stdin-error diagnostic limitations, and hypothetical provider/host changes; none establishes a new current defect. The strict CLI helper continues to fail on every unsuccessful write and never discards that failure as success. Requested policy and observed protection remain separate, including the corrected TUI wording. External reviews attested 58/58 captured pages but omitted docs/macos-support-plan.md (111392 bytes); native provider/source context was reviewed by Codex selectively. Model reviews are not independent security review.

Final native run https://github.com/bpcakes/jury/actions/runs/33991870490 at 2c00dd0eb8ca529668bb595bbebebf57a93c843b passed all four lanes and full Linux gates. Provider tests: 112 each Mac /164 Linux. Protected tests: 42 each Mac /41 Linux. TUI: five each. Strict CLI capture passed alone after the hosted shell set nonzero core limits, and in a local Linux container with initial CORE 1048576:1048576. Windows protected-test compilation and 42 native Nextest cases remain valid for unchanged protected implementation. Main workspace implementation/docs matched the validation candidate; main remains uncommitted. The temporary workflow was deleted and pushed as cf881ea96147492937df8e3c8f2c85ef33b9d502 after logs/results were retained. M01 was closed under the existing authorized early activation. The intermittent transfer snapshot inequality remains explicitly unresolved as jury-sqn, with stronger value-free diagnostics and no assertion weakening; it is not described as fixed by the passing reruns. Final repository gates are refreshed after tracker updates before this plan is finished.
