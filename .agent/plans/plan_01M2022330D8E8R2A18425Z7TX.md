Consumer: maintainers integrating upstream rollover with completed M01/M02 support. Verify the merged source and repair observed platform boundary regressions, preserving staged review fixes. Run provider checks, filesystem native image acceptance, affected portable crates, Linux workspace regressions, and report native CLI limitations. Retain this plan until reconciliation validation is superseded by release evidence.

Reconciled origin/main cd8eeac with local macOS history in merge 099d994, preserving both parent histories. Restored the saved staged fixes byte-for-byte and retained all upstream and local append-only journal records. Beads normal auto-import rejected upstream J18 normalization; lossless br sync --reconcile updated that one record and preserved all local rows/events.

Observed integration defect: new HardenedStateRoot::confirm_private_cleanup_absent called ordinary sync_all directly. It now uses platform::sync_parent, preserving F_FULLFSYNC on Darwin and ordinary sync on Linux. Existing injected failure/reappearance tests remain; a new regression proves cleanup confirmation uses the retained directory after its path is renamed and replaced.

Native macOS 26.6.2 arm64 / Rust 1.98.1: 63 filesystem tests pass on each owned APFS case mode, plus the explicit HFS+ refusal test. Provider workspace/selected-profile tests total 240 passes (two existing ignored), provider formatting and Clippy pass. Protected/core/protocol tests total 285 passes (one existing ignored measurement), and all-target/all-feature Clippy for those crates and filesystem passes. Rollover bindings/conformance, suite-2 provider/source bindings and verifier regressions pass; alternate-provider suite-2 conformance was not rerun. Repo format/contract and exact-upstream file budget pass without changing policy. Linux release packaging tests pass (10 tests).

Full native scripts/jig check test still fails at pre-existing Linux-only rustix memfd imports in execution_commands.rs; no complete macOS CLI support or macOS 15/Intel acceptance is claimed. Jig evidence/gates/finish were inspected and the plan remains open: the full native workspace profile is not green. Linux workspace Clippy passes; the complete Linux ARM64 workspace test/doctest run passed 506 tests with zero failures and 2 existing ignored tests. All 25 CLI integration tests and all three self-hosted witness integration tests pass. The focused Linux cleanup run also passed both cleanup confirmation tests.


The platform fix and owning regression are committed as 635f078. The merge is 099d994; no push was performed. Pre-existing staged review-fix code remains staged and unchanged. Validation used the combined working tree, including those staged fixes. Recovery stash was removed only after byte and journal-record preservation checks. Agent-map and action-pin checks pass. The remaining uncommitted additions for this reconciliation are the plan, Jig receipts, and an M02 tracker comment.


Follow-up pull reconciliation: the operator authorized committing all pending changes after origin/main advanced to 52ab5f1. Commit 66805c5 captures the reviewed file-budget changes and earlier validation evidence. The new source changes merge without conflicts. The Beads conflict combines the local M02 validation comments with the upstream repository-path change; after tracker reconciliation/export, all issue IDs and comment content from both parents remain present (local SQLite comment IDs are normalized). Append-only journal records from both parents were verified as retained.

Focused merged-tree validation: native macOS filesystem suite passes 63 tests with two existing ignored tests; J25 tests pass three with one existing ignored measurement. Format, Jig contract, and exact-pre-pull file-budget checks pass. The changed TLS certificate refusal/HTTPS lifecycle test and transfer snapshot regression pass in Linux ARM64. The witness integration target is Linux-only and executes zero tests on macOS; no native witness pass is claimed. Full native scripts/jig check test still fails at the unchanged CLI memfd imports. The first diagnostic invocation used its default release-binary location, which is absent in this container; the corrected invocation explicitly selects the built /target/debug/jury and juryd.

The corrected Linux diagnostics completed successfully, including the new rejected/accepted drill state-parent cases, restored-value readback, service credential refusals, and approved run/exec policy timeouts. These are focused checks on the new upstream delta; the earlier 506-test full Linux result applies to the preceding reconciliation, not a fresh full run of 52ab5f1. No additional runtime change was needed for this merge.
