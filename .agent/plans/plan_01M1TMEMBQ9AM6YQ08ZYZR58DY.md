# Repair the remaining Linux 0.0.1 audit findings

Consumer: the maintainer preparing Linux 0.0.1. This plan gates the concrete item-creation, passphrase-input, witnessed onboarding/approval, diagnostics, and release-candidate changes justified by docs/qa-linux-0.0.1.md. Supersede it when these fixes and their separate reviews are complete; it is not security certification.

## Progress
- [x] QA-09: explicit passphrase stdin cannot become field data under an inherited environment source; exact-byte native CLI regressions and separate comprehensive review.
- [x] QA-08: create additional items in a vault containing witnessed-only items while preserving confidential descriptors and global name uniqueness; real mixed-authority lifecycle and separate review.
- [x] QA-04: runnable synthetic operator onboarding, endpoint/file/foreground help; execute the recipe and review separately.
- [x] QA-05: authenticated human summary before complete lossless approval review; PTY checks and separate review.
- [x] QA-06: actionable safe filesystem/configuration/recovery diagnostics and policy-compatible witnessed run default; actual refusals/success controls and separate review.
- [x] QA-07: consistent JSON parser/domain errors and externally-unreviewed pre-alpha messaging; native CLI checks and separate review.
- [x] QA-10: real role-state bootstrap for optional multi-role backup (jury-34k), then native backup/recovery and separate review.
- [x] QA-11: shared witness pairs across multiple item policies; repair the checkpoint lifecycle through applicable protocol gates and real CLI validation.
- [x] QA-12: witnessed child stdin builds one authenticated field target; exact native stdin comparison and separate review (jury-wmo).
- [x] QA-13: governed owner changes on witnessed-only vaults (jury-tsc); real grant/revoke, denied/dry-run controls and both available review rounds are complete.
- [x] Local Linux release preparation: consistent 0.0.1 version, tested Linux x86_64/Debian 12 baseline, concrete verified package and separate reviews complete. J26 publication remains open for the private reporting contact, signing identity and publication; it is not completed by this repair plan.
- [x] Combined verification: actual direct/witnessed and recovery workflows, full tests, all required Jig gates and final package verification passed. Evidence and remaining publication limits are recorded below.

## Surprises & Discoveries
The previous QA-01/02/03 fixes are uncommitted and already verified; preserve them. Current QA-08 calls direct-only descriptor discovery to enforce global name uniqueness. Do not bypass this check or introduce a unilateral slot. QA-09 environment input currently wins even when --passphrase-stdin is explicit.

## Decision Log
Work on each finding separately. Use isolated review copies whose baseline includes the existing application state, so each review covers only its intended change. Default comprehensive reviewers are Claude and native Codex, maximum two rounds per finding; record terminal provider failures truthfully and address actionable findings. Local task tracking and this plan retain progress across long work; no application commit or push is implied.

## Outcomes & Retrospective
QA-01 through QA-13 are repaired and verified. Separate reviews and their provider limitations are recorded below. The exact local Linux 0.0.1 candidate passes real CLI journeys and required checks. J26 remains open for publication setup; no release has been signed or published.

## Context and verification
Read root AGENTS.md, agent-map.md, and crates/AGENTS.md. Audit reproductions are in docs/qa-linux-0.0.1.md and the named /tmp diagnostic scripts. CLI implementation is crates/jury/src/cli and secret_input.rs; domain and manifest checks are jury-core/jury-protocol; actual daemons are jury-witness. Preserve all cryptographic gate and persisted-format requirements. Use only synthetic actors and compare private outputs without printing them.

For each fix, add meaningful native regression checks with the implementation, run targeted tests and the actual reproducer, freeze the review scope using the comprehensive-review skill fingerprint helper, consume the selected frozen reports, and repair validated issues. Run a second review only when changes or unresolved findings justify it. Retain at most two rounds per finding. Run scripts/jig work check, work evidence, work gates, and work finish after implementation; finish backend work with scripts/jig check test. Review the final diff for stale docs and dependent updates. Do not count a narrow check as proof of the full release lifecycle.

## QA-09 review and validation
Round 1 reviewed isolated baseline 9d810b5d36afe3dfd58a68a047b6952327c09e4d in /tmp/jury-qa09-review-21ago90i, fingerprint fe74de48661c2999668ff81fe78f70cc99a5a02e9a7a6cb841a2a09d3a2b1f79. Both parent captures and both reviewers matched complete fingerprints. Codex found no actionable issue. Claude identified ambiguous opt-in/terminal guidance, missing field-flow/spec documentation, and a weak error assertion. These were addressed with consistent all-prompts guidance, README/master-plan updates, exact authentication-error checking, and an incomplete-backup-input regression. The terminal example in Claude's report used public vault status, which does not unlock; its broader documentation concern remains applicable. Native regressions passed (2 tests, 120.53 seconds) before the guidance/assertion follow-up; secret-input unit tests passed (3). The first invocation accidentally filtered the main binary and ran zero tests; it is not counted. Final follow-up tests and round 2 are pending.

Round 2 used fingerprint ddbd45c915b71f62b05411459d32339bb62eeb1610122cb71ff3a085a5270485; both reviewers and parent captures matched complete fingerprints. Codex found no actionable issue. Claude identified global stdin buffering consuming a later exec child's input and retaining a buffered passphrase, terminal blocking ambiguity, partial multi-role restore behavior, generic EOF classification, and incomplete prompt-order guidance. After the completed second review, the stdin path now reads exactly to the newline through the raw descriptor; approval/genesis confirmation input also avoids read-ahead. EOF has a separate actionable exit-2 error. README/recovery guidance states terminal blocking, exact command prompt sequences, and recoverable partial-restore behavior. No third review will be run for QA-09; these follow-ups receive local validation.

The new exact child-stdin regression passed, as did the real hidden-prompt PTY check and three secret-input unit tests. The attempted multi-role native regression found a separate pre-existing onboarding gap (QA-10, jury-34k): backup cannot find the registered approver's local state. It was not counted as passing or replaced with a fake native fixture. Owner-only native coverage remains, and existing role-recovery fault tests are being run as unit-level evidence only.

Final QA-09 native regression run passed all 3 tests (120.10 seconds), including exact inherited child stdin, equal/different/absent inherited passphrase sources, wrong-stdin rejection without mutation, incomplete backup refusal, and owner backup/restore with conflicting inherited sources. The hidden-prompt PTY check passed against the rebuilt binary. No real private material was used or printed.

QA-09 supplemental recovery unit tests passed all 7 cases (260.82 seconds), including injected cross-directory failures and all included identity roles. These are unit evidence, not a claim that QA-10 native onboarding works.


## QA-08 first review and adjudication
Round 1: isolated HEAD d6befcedcceebbd2459ba70ed22b6f97576d15de, fingerprint b84f9c632d1a2dcf08e12efdce4292d7f3d64c561be7aff3ec69c70644943ee6; both reviewers and parent captures matched complete fingerprints. Codex found no actionable defect and named negative/race coverage gaps. Claude found a critical authority-label mismatch: the proposed automatic descriptor flag emits a v1 item-only target that also permits whole-body reads. The existing protocol deliberately supports that shape, so no frozen rule, vector, or security gate is weakened. The unshipped option is renamed --automatic-read-item and explicitly describes permission for BOTH descriptor and entire body; there is no descriptor-only automatic permission in v1. Field-only policies continue to refuse descriptor reads. The name check itself still opens only descriptors.

Claude also identified content-role visibility, avoidable local-refusal witness reads, sequential/retry costs, and claimed output alias preflight gaps. The authenticated approval now prints DESCRIPTOR or BODY scope before the complete manifest; signatures already bind the entire manifest including content_role, so decision replay into another role is rejected by the existing binding. Creation prepares local inputs/capacity before witnessing and locally visible duplicates refuse before contact. A total approval-wait budget (default 300, maximum 900 seconds) bounds the sequential batch, with per-entry waits capped by the remainder. Documentation names O(N) reads and full retry cost; public receipts cannot resume a private session, so they are not accepted as reusable access. Transport calls already in progress can finish after the wait budget. The suggested symlink/text-alias issue is unsupported for the supplied example: preview_public_file opens every absolute parent component without following symlinks and rejects traversal; PathBuf equality normalizes equivalent components. No weaker canonicalize-and-follow path was introduced.

The real Linux multi-descriptor test passed with two governed item policies using distinct witness pairs, including pending approval, signed denial, duplicate names, dry run, unchanged governed envelopes, creation, and receipts. Using the same pair for both policies separately reproduced QA-11 (jury-r3l), so that is not claimed repaired by QA-08. Expanded field-only refusal and mixed direct/governed checks are pending on the revised release binary.

Important remaining QA-08 limitation: the exact original field-only automatic policy still refuses descriptor authorization after the cutover. The new path succeeds only when the existing policy grants descriptor access (human-approved read or explicit whole-item automatic authority). Do not close QA-08 or claim the original reproduction fully repaired solely because its refusal is now actionable. Resolve the lifecycle/policy design while preserving descriptor confidentiality and name uniqueness; refusal-only work remains open under AGENTS.md.

QA-04 draft verification: executed the exact Python block extracted from docs/witness-operator-walkthrough.md against two live release juryd instances. Exact registration, witnessed read/receipt, restart/replay/cancellation, a subsequent vault mutation, chained checkpoint operator POSTs and durably-accepted status passed. The updated code also rejects malformed credential bytes without reflecting them or contacting a service. The first negative harness used operator.py and shadowed Python's stdlib operator module; corrected to the documented operator-post.py name. Real release help for read/inject/run/exec and request create/execute contains the new file/wait/session guidance. First separate comprehensive review is running; no QA-04 completion claim yet.


## User-authorized hard cutover
The user explicitly clarified that Jury has never been released and no real vaults exist: perform a hard cutover, without compatibility or migration paths. This supersedes earlier compatibility assumptions. Redesign the descriptor-authority and per-vault multi-policy checkpoint contracts directly, update/freeze and verify the applicable protocol inputs before implementation, and update fixtures/tests for the implemented contract. Do not retain the broad --automatic-read-item workaround merely to preserve old semantics. Gate rebinding must be truthful and exact, not a bypass or a spec-only substitute for capability.

## QA-08 second review and follow-up
Round 2 used fingerprint b4809bc50248decc85a283f98d362c860bfcc82f959e23b2a73f571fef04434e on the same isolated baseline. Claude and Codex completed; all captures matched complete fingerprints. Codex identified missing propagation of the absolute batch deadline through reservations, approvals and decisions. Claude identified broad automatic item authority, discarding completed authorization at a final deadline check, an aggregate wait cap too small for sequential approval, late count validation, and missing dry-run/absolute-path guidance. Deadline checks now occur before new external calls and approval iterations; the completed final authorization is retained. Aggregate wait is explicitly configurable up to 86400 seconds, with each approval wait still capped at 900. Count validation and help were corrected. Slow-endpoint regression remains pending. The user-authorized protocol hard cutover will replace the broad-authority workaround, so QA-08 remains open. Both comprehensive rounds have been consumed; do not use relabeling to obtain a third pass.
The real lifecycle and descriptor drivers passed against the release binaries before these final deadline edits. All 42 witness-engine unit tests passed. These do not prove the pending protocol redesign.

## QA-04 first review
Claude and Codex completed round 1 on isolated HEAD dc24b23feb1f1c7124dc1c5518060c4970660483, fingerprint c6456990a8e262c9f2dc3b93ef38331fa0f865658b9b1eb5e036e806b40251f7, with matching complete captures. Claude's initial adapter invocation failed before starting the provider because of a stray argument; the corrected invocation completed on the same scope. Findings concerned 0700 artifact parents, required HTTPS CA files, the approver share-index copy/paste branch, trailing newline credentials, newest-checkpoint usage, JSON status spelling, URL diagnostics, principal-ID provenance, and fresh-host onboarding context. The guide and help are corrected; native separate-XDG bootstrap and the exact documented operator code are being rerun before the second and final review.

QA-04 round 2 completed with Claude and Codex on isolated HEAD a562c7bdf020917824531b7caebca5e19433d0ca, fingerprint 42c14566c4f8a51993e33deb8f85696bf3e333e379331d1a03ebdebdba3746c3; both parent captures matched complete fingerprints. Codex identified missing remote-approver post-policy provisioning. Claude identified terminal confirmation guidance, late wait-range validation, credential-file and loopback parity, field-size/variable/mandatory-flag guidance, and cancellation semantics. These were repaired after the second/final review. Separate-host initial registration was executed successfully with separate XDG roots and only the current encrypted vault snapshot; later approval is explicitly scoped to the original installation until QA-10 adds current authenticated role/catalog bootstrap. The guide does not claim remote approval now works.
Final exact documented operator code passed against live release witnesses: registration, read and receipt, restart, cancellation, chained checkpoint POSTs and durably-accepted status. Safe negative checks cover malformed/missing hosts, noncanonical loopback, absent HTTPS CA, malformed tokens, and permissive credential files. The parser regression passed 15 cases across five surfaces (0 and 900 accepted, 901 refused before execution). No third comprehensive pass is claimed; the final fixes were locally verified.
QA-01 hard-cutover follow-up: all three replay-storage tests passed and the rebuilt release binaries passed the actual Linux witness lifecycle, including initialized empty databases, read, receipt, replay, restart and cancellation.

QA-05 first runtime pass completed read, private-file, inject, run, exec and denial through real approval PTYs, including escaped newline/ESC arguments and exact full request/manifest/presentation comparisons. Clippy passed. Round 1 Claude/Codex completed on HEAD 8689259c4732a9ba66fd76e4cea92011ffa174c3, fingerprint 9974829bbf2c20e6f0b3561972ab0b5474f4018671798a04607fd4da3273eee6, complete and unchanged. Claude identified missing environment/stdin summary channels, inconsistent non-ASCII byte rendering, ambiguous label-only item identity, and test hygiene/durability gaps. Codex found no production defect and identified incomplete summary-value assertions and an exec environment oracle that did not distinguish missing injection. Repairs and second review are pending.

QA-05 expanded stdin coverage exposed QA-12 before publishing a request. Do not remove the failed case or claim stdin validated. The separate QA-12 correction omits the extra item target only for ChildStdin and retains the frozen one-target validation. The field label already authenticates its item ID. QA-05 now also compares exact injected environment/stdin bytes through a child cmp against a synthetic private fixture file, without printing the value. QA-06 (jury-tx2) has started safe filesystem/restore diagnostic edits, not yet tested or reviewed.

QA-06 native diagnostics passed twice, including the added recovery layout controls: unsafe 0664 template refuses with an ownership/mode constraint and unchanged output, 0600 succeeds with exact bytes; schema/path/authority-label/unknown-field daemon configurations fail safely, a valid database initializes; default run emits the policy's 5000 ms timeout and reaches pending approval, while explicit 6000 ms refuses before publication and no unapproved child runs. Four existing config unit tests and scoped clippy passed. Current default also preserves the broker's 24-hour ceiling; direct default remains 1800 seconds. Updated recovery examples to use separate actual parent trees and the restored state root. QA-06 first review is running on HEAD 649f57600c968a9c709212ff7ca34853ed4fcb4d, fingerprint 0074f524928951aa4885a4e9612505c6c69ad169d8bd9cda40f3bb24579d395f.

QA-05 second/final review completed on HEAD ece7aef08d7bf96ddec4332ebbc0400c70cf4d49, fingerprint 0f43f90cc90b529806774af4f1916db5f88de2e300f70e9103c4b893a0affed5; Claude and Codex and both parent captures matched complete fingerprints. Codex found no actionable defect. Claude found inconsistent byte escaping for requester-chosen paths and the pre-existing ability to redirect stderr while confirming a hidden review. These are corrected; the summary also includes witness IDs, platform assurance and not-before when present. Added actual confusable cwd/output and redirected-stderr refusal cases. Claude's request for a duplicate unit summary fixture is retained as a test-layer gap rather than a medium production finding: real Linux CLI/PTY tests run in CI and are the intended supported surface; fmt/clippy jobs are intentionally not test jobs. No third comprehensive pass for QA-05.

QA-12 round 1 completed on HEAD 640eaeb131ff1a8f49b3c7d730e0f7a16bfbde10, fingerprint d3c8c4113144a6e6435ed01e973db20eed76407e050a60b71b83898e526b032f, complete/unchanged. Codex found no actionable issue; Claude confirmed the one-field builder fix but identified the loss of the human-readable item label. CompleteApprovalReview now retains owner item labels only after exact-set and signature verification, exposes those authenticated labels to the summary, and includes them in full rendered context. The builder rejects multiple stdin fields early. cmp is an explicit test prerequisite. The existing human action unit test retains all template assertions and now validates the stdin manifest, authenticated supplementary label, forged-label rejection, and two-field rejection; it passed. Generalizing unused backup/recovery action builders is not part of this fix. Second QA-12 review remains pending.

QA-05 final native approval driver passed after all second-review repairs, including exact environment/stdin byte comparisons, authenticated item labels, confusable-path escaping, and refusal when stderr hides the review. Scoped all-target clippy for jury, jury-core and jury-witness passed. QA-05 is complete; QA-12 separately retains the stdin builder/domain change and its second review. QA-12 round 2 is frozen on HEAD 4a96b0da918d95f5c4f0978262c648a8e8655f00, fingerprint 31527273d7f1de0bb90ed83446c5d8be44daf5915a74c6362d4298dbb02e0b94.

QA-12 round 2 Claude/Codex completed on fingerprint 31527273d7f1de0bb90ed83446c5d8be44daf5915a74c6362d4298dbb02e0b94; both parent captures complete and unchanged. Codex found no actionable issue and ran the core test. Claude identified ambiguous supplementary aliases, silent missing labels, and literal ellipsis refusal. Final repairs show alias IDs/revisions, require item labels in the builder and fallback reviewer, expose supplementary labels consistently only when the presentation has no item entry, and encode literal ellipses losslessly in JSON. Existing core regression now covers two signed aliases, decoded ellipsis equality, missing label refusal, exact forged-label error, and distinct label IDs/revisions; it passed. Claude's claimed cargo-fmt gate failure was unsupported because this source is include!-included and cargo fmt ignores it; the changed test function was nevertheless formatted directly without unrelated churn. Native final regression remains running. Both rounds are consumed; no third review.

QA-06 round 1 complete: Claude/Codex and parent captures matched complete fingerprint 0074f524928951aa4885a4e9612505c6c69ad169d8bd9cda40f3bb24579d395f. The Claude forwarding host turn hit capacity, but the original provider process was recovered and completed without a duplicate invocation. Codex found no actionable issue. Claude identified drill/source boundary wording and stale test assertion, misleading missing-operation classification, overly large default with generous policies, private credential diagnostics, and output-limit diagnostics. Repairs name source boundaries, classify missing child rules as access denied, cap the default at min(policy, 1800 seconds), add safe private-file/token messages, and preflight output/target limits. The policy lookup remains for transparent exec because it now provides operation/workload diagnostics there too. No compatibility shim is retained for old error codes. Credential unit tests passed; expanded native checks include actual service-start file failures and approved runtime termination, currently running. An initial output-limit probe exceeded the broker's own 1 MiB bound before the policy check; it was corrected to assert an actionable broker-bound diagnostic rather than presented as a policy-bound test.

QA-07 first native parser/help tests passed (2); real direct field-redaction and complete identity/vault/access/execution/plaintext lifecycle tests passed. First separate review is still collecting its selected reports. Do not count a partial review as merged.

QA-12 final native approval driver passed after round-2 repairs, including witnessed stdin, exact delivered bytes, full review, and verified receipts. QA-12 is complete. QA-06 expanded native driver passed including approved default timeout termination, no surviving child PID, verified authorization receipt, service-start credential failures and drill/source containment. QA-07 round 1 completed with matching complete fingerprints. Codex reported no actionable issue; its broader native suite passed 15/16, with the separately known QA-06 stale backup error-code assertion failing (now corrected in the main workspace). Claude identified stale exec/run help and error-envelope duplication, both repaired. Its claim that Clap accepts --json=true/false was disproved by actual CLI calls: both are rejected as unexpected values. Added explicit regression/documentation for standalone --json. Cosmetic repeated maturity warning if a plaintext write itself fails remains harmless; centralizing at startup would obscure the approval scope first line. No new approval flow was introduced.

QA-06 final second review completed with Claude/Codex and matching complete parent fingerprints 8055134e12ac1ee75fca55845579bf7aa471b4e1466e973565ec1de70fdb5004. Codex found no introduced defect. Claude identified unbounded witnessed exec, missing absolute-path wording, and a five-second approval-lifetime race. Witnessed exec now uses the policy-bounded default; native run/exec each requested and enforced 30000 ms with a 300-second approval lifetime, terminated the child, and verified the receipt. Path and restored-state follow-up guidance is corrected. Claude's stdin-count claim reflects the isolated baseline before the separately completed QA-12 fix; the final one-field count is correct. Shared PrivateFile diagnostics intentionally cover ownership/type/link/permission/readability constraints; tests use valid tokens, so accepted links would not be disguised by malformed content. Exact source-vault backup drill/restore regression passed (175.83 seconds). Both QA-06 rounds consumed; no third review.

QA-07 second/final review completed with matching complete fingerprints d405b97e85426d8211c2c6820a441579eb959848eb00a049ee12448c4f7f912d. Codex found no actionable issue. Claude found ambiguity for malformed --json= values, overly broad README stream wording, sibling maturity wording and stderr-write panics. Malformed --json= now selects a static JSON refusal; the flag still rejects values. The shared error writer tolerates unwritable stderr without changing exit status. Shared MATURITY/juryd help include externally unreviewed; docs distinguish human metadata stdout from streaming notices. All five focused native diagnostics tests passed, including successful JSON and unreadable stderr. Two rounds consumed; no third review.

QA-08 protocol hard cutover: local input-only commit c427bdc freezes explicit descriptor/body automatic targets and rejects automatic whole-body/legacy role-implicit shapes. It changes only six protocol/conformance input files; all 34 prior cryptographic vectors and prior mutation/scope/protocol/crash/retention data remained exact. Added two canonical automatic-target vectors and nine concrete automatic cases (19 presentation cases total). Rust consumer, alternate Python consumer, canonical mutation rejection and conformance Clippy passed. Updated J19 binding to this reachable Git revision; scripts/check-witness-gate passed all unchanged required checks before runtime implementation began. This is the required fresh solo input verification, not independent review or a third comprehensive pass. Application changes remain uncommitted. Runtime target-role matching and narrow CLI permission selection are now being implemented; native lifecycle is still pending.


QA-08 final hard-cutover verification passed. Native automatic policy selection creates explicit descriptor(None) and body(exact field) targets, returns exact allowed field bytes, refuses another field without publishing authority, and creates subsequent items without altering governed envelopes. A forwarding proxy delays an actual witness response beyond the total batch budget; Jury starts no second witness call, publishes no success receipt, and leaves the vault unchanged. The real two-policy human-approved driver passed coverage, pending/denial, duplicate, dry-run and creation cases with verified receipts. A positional item-array test assertion was corrected to compare whole retained records independent of random ID ordering. Core suite passed 133 tests with the release measurement test explicitly ignored; scoped Clippy passed.

The combined native lifecycle exposed a QA-12 follow-up regression: automatic request cancellation was incorrectly required to have a supplementary human item label. The renderer now requires those labels only for authenticated human presentations. The focused automatic request review and existing human stdin/label regressions both passed. Rebuilt release binaries passed the full Linux witness lifecycle, including automatic read, receipts, stable replay, daemon restart, cancellation and governed name checks. Formatting and scoped all-target Clippy passed after the repair. QA-08 and QA-12 are complete; both had already consumed two comprehensive rounds, so these final repairs received local validation, not an extra review pass.

Hard-cutover follow-up still required: remove the unreleased legacy backup-receipt coverage variant and catalog compatibility parsing/default behavior. This is a concrete user-requested format cleanup, not a migration task; preserve absence of a backup receipt when no backup exists.


Unreleased receipt/catalog hard cutover (jury-j5j) completed. Removed BackupReceiptDetails::LegacyV1, optional coverage parsing, the old payload-only MAC-entry fallback, and catalog omitted-field defaults. A missing backup receipt still means no backup exists. Receipt and catalog versions remain 1 because no version has been released. Docs explicitly state rejection of scratch catalogs/receipts and archives embedding old receipts, with fresh synthetic data and no migration path.

Separate comprehensive round 1 used isolated HEAD c779e273fe427bf3e6247e51c019f2b5bbd949a7, fingerprint ddaa3c05c9d86846a77b47adeba51aea63187d21c129993668ca0ab6aff2442b, complete/unchanged for both reviewers and parent. Codex found no production defect and correctly identified malformed catalog fixtures masked by missing required fields; repaired those fixtures. Claude's compatibility/version-bump findings conflict with the explicit user instruction and unreleased state, so no fallback/version migration was added. Its catalog/archive documentation gap was repaired. Added a fixed independently calculated Python SHA-256 coverage digest and exact ReceiptEntry comparison, plus per-field missing-coverage refusals, addressing the useful oracle gap without claiming the old legacy fixture had pinned coverage (it had not). The optional status fields remain meaningful when no backup exists; Claude's claim that their absent arm is unreachable was incorrect. Only tests and docs changed after review, so no second pass was needed.

Validation: 8 original local-state tests passed; final expanded local-state filter passed 11 including the independent digest and owner backup round trip; 2 catalog tests passed; native real backup/restore/drill passed (178.63 seconds); portable transfer/first-install/retry/rollback suite passed (138.91 seconds); rebuilt release native descriptor driver passed automatic and human policies with nonempty catalogs, actual witness daemons, exact payload assertions and receipts. Formatting, scoped all-target Clippy and diff checks passed. The initial catalog test command accidentally selected the binary and ran zero tests; it is not counted, and the corrected library target ran both tests successfully.


Owner-slot history cutover (jury-jfc) is in progress. Replay now rejects the same incomplete implicit owner descriptor/body slot sets as new writes, for every authenticated revision. It retains the witnessed-only exemption. First comprehensive round used isolated HEAD 303f84b7bc8ff8661a1198a8c332fdb7d2d4fda4 and complete/unchanged fingerprint c4a16c87c3ee70c61c2866eb802c881633e5785ffd8f7745a1afc7ef5f01e4cc. Both selected reviews and parent captures completed; the Claude adapter initially rejected a punctuation argument before invoking its provider, then the corrected original round ran once. Codex found no actionable defect. Claude identified repeated owner-by-slot scan cost during replay, state-hash error precedence, and weak attribution of the negative test. Fixed these: index slot coverage once per item with BTreeMap (logarithmic lookup, no owner-by-slot rescan); check committed state hash first; assert accepted prefix and distinct signed state-hash mismatch before the missing-slot refusal. No escape hatch or historical repair path is added: this is the explicit hard cutover, and the writer already checks the same invariant before publishing. Existing historical plan text is superseded by the user's instruction and current decision log.

Before the indexing follow-up, 29 policy-filtered tests and all 134 active core tests passed (one dedicated release measurement ignored), plus the real Linux witness lifecycle. After follow-up, 12 policy tests, the multi-item owner grant/revoke core test, formatting and scoped all-target Clippy passed. Expanded real CLI lifecycle now creates a second human identity, registers it, grants owner authority, checks exactly both owner slots in the signed policy operation, reads exact bytes as that owner, revokes it, checks slot removal and retained owner data, then completes witnessed read/replay/restart/cancel/name-check flows. This passed against rebuilt release binaries. Initial test-driver errors looked for slots on the encrypted envelope instead of policy operations and omitted the explicitly required --direct flag; corrected actual commands/assertions, with no product workaround or weakened expected success.

Second and final owner-slot review is running on the same isolated baseline, fingerprint 89040efaa8ae91d4761d6acc10b6b31a98aede1e8441e07679a5843c5b08f95b, complete/no issues. Await BOTH owner_cutover_claude_r2 and owner_cutover_codex_r2 and recapture parent fingerprint before merging. No third round. Tracker remains in progress until final adjudication and any necessary validation.


Owner-slot cutover round 2 completed with Claude/Codex and complete unchanged parent fingerprints 89040efaa8ae91d4761d6acc10b6b31a98aede1e8441e07679a5843c5b08f95b. Both found the core implementation correct. Codex had no actionable finding. Added the meaningful shared test gap: a revoked owner now attempts a real private-file read, receives exact item-unavailable/exit 6, and publishes no output. Claude's claim that the lifecycle never exercises WitnessedOnly replay is incorrect: the driver converts the item then runs all witnessed operations; flipping the exemption would fail that flow. Its separate owner-change limitation was real and is now QA-13/jury-tsc: grant-owner after witnessed-only conversion returns invalid-vault/exit 1. The driver asserts unchanged vault bytes for that known refusal and labels direct owner-change coverage explicitly; no unsupported witnessed owner-change success is claimed. Mixed negative/duplicate-role shapes remain nonblocking test gaps; current core tests cover successful mixed/witnessed-only replay. The wrong-state-hash assertion intentionally pins precedence over missing slots and is retained. No third review.

Final owner-slot native lifecycle passed after these test-only follow-ups: direct owner grant/revoke, exact granted read, denied revoked read/no output, known witnessed owner-change refusal, actual witnessed read/receipt/replay/restart/cancellation/name checks. jury-jfc is complete. The repository full test run failed 54/55 CLI tests solely on the stale QA-08 help assertion 'automatic read-stdout'. Updated that assertion to require the implemented --automatic-read FIELD and --automatic-descriptor narrow permission wording and absence of --automatic-read-item; the focused help test, formatting and diff checks passed. A fresh scripts/jig check test is running (exec session 22587); do not claim full-suite success until its terminal result is collected.


Stable-tree verification: the earlier full Rust run exited 0 but Jig correctly rejected its changed worktree fingerprint (test-only help assertion split during execution); no gate pass was claimed. The fresh scripts/jig check test completed successfully in 842.3 seconds, plan run-plan_sha256:3d162d012f4f6452b72b381ed42bb7738d8d451e6ef1a6c5692d9d7f49346d9d. This verifies the main tree before QA-10 integration.

QA-10 is isolated in /tmp/jury-qa10-work-hkdfumth. Round 1 Claude/Codex and both parent captures completed on HEAD 9b3a24ec6d3715805b72305793c00f31025d3d1d and complete unchanged fingerprint d0a498ed7750fd18519021b2710dfeea26e6aca986bd56623866948ed8674fca. Codex reproduced the existing pre-registration snapshot failure: the loader required registration in the old local policy. Fix in progress uses the authenticated incoming transfer registration while preserving current local checkpoint and ancestry validation. Claude identified role inspect inconsistency, directory-blind snapshots, source-path shortcuts in remote updates and missing request/approval delivery guidance; those are being corrected. The speculative future generic-commit misuse is not a current defect and does not justify an unused trait. Native Linux process coverage is the supported CI surface; it is not mocked proof or independent security review. Missing Rust-layer role cases and exact-descriptor collision tests remain distinct test gaps, not evidence of a demonstrated bypass.

Before round-1 fixes, all 56 CLI library tests passed and the native portable transfer/preview/conflict/retry/rollback test passed in 140.49 seconds. An initial --exact invocation selected zero tests and is not counted. Real separate-installation human approval, exact witnessed output, verified receipt, signed role updates and rollback refusal passed within the new driver, but final recovery had a fixture passphrase reuse failure. The next driver uses distinct backup and identity passphrases with the actual five/two/seven line create/verify/restore prompt contract. The finding remains open until the full driver and final review finish.


QA-10 final review and validation: round 2 Claude/Codex and both parent captures completed on HEAD 9b3a24ec6d3715805b72305793c00f31025d3d1d, complete unchanged fingerprint 4b560b932ae198e2eb66a79019705e5975c4ded0cb286e8592233088dda3b8ef. Codex found no actionable defect. Claude's live-service snapshot race was repaired by excluding only the separately owned services directory; directories, modes and application state remain checked. Recovery guidance now explains that authenticated incoming state can precede public publication, with exact/descendant retry and strict sibling refusal. A real signed sibling is rejected after the injected durable-intent failure, then the exact transfer retries and reconciles successfully. No checkpoint/fork invariant was weakened. The suggestion to stage an untracked reviewed script is not a runtime defect and does not authorize an application commit. Optional inspect capability labeling and CLI process-crash injection remain test/UX opportunities; role inspection correctly unlocks without exposing private names. No third comprehensive review.

Final real Linux role driver passed, including separate fresh approver and witness installations, pre-registration snapshots, retained-state repair of an older public snapshot, missing-public-file rollback refusal/recovery, remote PTY approval, exact witnessed output/receipt, current signed updates, role authorization refusal and three-role backup/restore with distinct backup passphrase. The focused durable-intent retry/sibling test passed (34.33 seconds); all 9 commit tests passed before the final sibling assertion. Formatting, scoped all-target Clippy and actual rust-file-loc gate passed. Full combined scripts/jig check test passed (842.3 seconds), run-plan_sha256:70ec7e3a6a7833aae4e037394124b24e538d454b6e313fabfe37aebf6fe03b8b, before final test/docs-only follow-ups; production code is unchanged by those follow-ups. QA-10 is complete; QA-11, QA-13 and release preparation remain open.


QA-11 input hard cutover: input-only commit 0fc37bd288a4fd896c3097b2be9712ab8d4c1775 freezes one checkpoint per vault revision with the exact canonical active policy-set digest. The six changed protocol/conformance inputs preserve all prior 48 byte-mutation classes, scope/presentation/split-write cases, the full retention model and 20 prior protocol cases. Checkpoint-dependent signatures and contribution ciphertexts changed as required by the new signed framing; they are not represented as unchanged vectors. Three new hash vectors and 22 concrete post-authentication rule cases give 39 vectors and 42 protocol cases. Rust consumer tests (3), independent Python consumer, deterministic generation check and conformance Clippy passed; scripts/check-witness-gate passed with its unchanged checks/providers and updated exact six hashes/counts/baseline before runtime edits. This is fresh solo input verification, not independent security review. Runtime work is isolated in /tmp/jury-qa11-work-jgows_5d on baseline 6b3f4ca; no QA-11 comprehensive rounds have run yet.


QA-11 first review started on isolated HEAD 6b3f4ca5fe9ee72d105b196dea075ed40907097b and complete fingerprint 34a21edbba027dd140e0c3b393fef5bcde42f5cccc9c8f7986c00ef80d024518. Runtime uses the exact active set and selected-policy request authority; old checkpoint/ack shapes are rejected. Real shared-pair automatic reads, receipts, unique propagation counts, signed sibling/rollback refusal, strict advance/idempotency, stale request refusal and restart passed. All 43 witness-engine tests and 12 protocol tests passed; an initial engine filter selected zero tests and is not counted. New cross-policy negative correctly pins existing StalePolicy classification from exact rule selection. Public status fixtures were recaptured through successful real current CLI/daemon operations after the wire cutover; no assertion was weakened. Frozen inputs remain unchanged. Runtime scope copied to main for full combined verification while both reviewers inspect the frozen isolated copy; final review/CLI distinct-values checks are pending.


QA-11 round 1 completed with Claude/Codex and unchanged complete parent captures (34a21edbba027dd140e0c3b393fef5bcde42f5cccc9c8f7986c00ef80d024518). Codex found no actionable defect. Claude identified wrong-slot refusal classification inconsistent with the frozen WrongScope cases, ambiguous repeated public labels, and repeated checkpoint policy validation. Runtime now matches the unchanged frozen cases, ambiguous labels give a specific ID-based remedy, and engine/receipt paths reuse the validated active policies. The proposed catalog first-slot defect is unsupported: replay requires every witnessed slot of an item to use the same terms. No catalog workaround was added.

The combined main test gate failed on an old checkpoint --item-id argument in the native witnessed workflow (run-plan_sha256:7cb2d6134876a971784488c6935abd68f6593511d23e7ba99c80f1bba57bc264). Removed that obsolete argument and unused helper inputs; the full affected native asynchronous approval/read/inject/execution test passed (258.53 seconds). Final shared and disjoint witness drivers both passed: two active policies, third-policy activation at strict next revision, exact distinct reads/receipts, unique witness union, wrong-policy witness refusal, signed sibling/rollback rejection, idempotent advance, stale request rejection, and daemon restart. The separate human descriptor driver passed repeated-label refusal and opaque-ID approved flows. Scoped all-target Clippy and the actual rust-file-loc gate passed. A repeated engine test command selected zero tests and is not counted; corrected witness_engine::tests selects 43.

QA-11 second/final comprehensive round started on HEAD 6b3f4ca5fe9ee72d105b196dea075ed40907097b and complete fingerprint f14bd638d0981df1aa94d6eeab7739d63d1393c7b94984d073aa185520525cf7. Both reviewers inspect the immutable isolated tree; 42 runtime/test/docs files were copied to main for fresh combined validation. No third comprehensive pass will be used. QA-11 remains open pending the final review and combined validation.


QA-11 second/final review completed: Claude and Codex plus parent captures matched complete fingerprint f14bd638d0981df1aa94d6eeab7739d63d1393c7b94984d073aa185520525cf7. Claude adapter exited 0 once with restricted access; inline output had no separate paged-evidence coverage block. Codex reproduced previously unused witnesses failing to join a chained global checkpoint. Removed the stale zero-predecessor registration restriction: the frozen registration-any-active-policy case already accepts a nonzero predecessor. Existing registration replay, strict advancement, owner lineage, current membership, fork and rollback checks remain. No frozen protocol input or expected case changed.

Claude found a stale fuzz checkpoint constructor, duplicate hashing/read work, eager unrelated-label loading for ID selectors and unclear empty-checkpoint output. Fixed those: current fuzz shape, reused verified policy-map digests, one preflight checkpoint read, lazy routing labels for opaque IDs, typed missing-label guidance, and an explicit active-policy count/empty-set explanation. Complete policy-set validation is required by the frozen contract; no partial-policy checkpoint fallback was added. The alleged items-times-policies complexity is incorrect: the walk is linear in total policy bytes plus item/set lookup costs, though its duplicate hashing was real and removed. No constant-time or new performance claim is made. The reported cargo-fmt gate failure was unsupported: both native review and current cargo fmt --check pass; include!-included files do not behave as inferred by that review. Existing security CI already compiles/runs the fuzz workspace through check-j25-fuzz-smoke, so no duplicate CI gate was introduced. Audit stayed open deliberately until repair validation, not because the work was claimed complete.

Final scoped verification passed: 44 witness-engine tests, all-target Clippy, formatting, actual rust-file-loc gate, fuzz workspace all-target compilation and 5 tests, and generation of the current checkpoint parser seed into a fresh temporary corpus. Shared and disjoint real CLI drivers both pass two initial policies then a third with previously unused witnesses, exact retry/anchor union, restart, exact reads/receipts, empty-set registration refusal, signed re-registration sibling refusal with unchanged status, rollback and stale-request refusal. The final descriptor driver passes repeated-label refusal, opaque-ID creation with an unrelated missing label set, typed missing-label refusal, actual human approvals/denial, creation and deadlines. An initial diagnostic test guessed exit 6; corrected to the existing NotFound exit 3 without changing the product error or weakening its exact code/artifact checks. Mixed-policy human approval substitution and maximum-size measurements remain specific test gaps; generic signed scope substitutions and the actual shared-pair human approval flow are covered. Both comprehensive rounds are consumed; no third pass.

Main full test gate passed before these final second-review repairs (874.8 seconds), run-plan_sha256:230cab362a3e5dbba9180446c5c54f29d1d09d5196e7961eb7acdcea4e2740eb. Final QA-11 files are now integrated; a fresh combined gate is still required before closing jury-r3l. QA-13 remains independently open.


QA-11 is complete. The final combined scripts/jig check test passed in 855.1 seconds, run-plan_sha256:e4f7d1595a13093bc4d083eeb754861c73ae2f59aa96c11fae2186e6830fd37a. This includes all second-review production repairs; both comprehensive rounds and the final real shared/disjoint/new-witness/descriptor journeys are complete. No remaining QA-11 medium-or-higher defect is being deferred as acceptance.

QA-13 is in progress in /tmp/jury-qa13-work-b3v9jash, baseline 54e3ec2eeaf3bd0800b9efd708304a3513b1d556. An explicit owner-change context under administrative-rekey authorization avoids claiming a commitment to future ciphertext before old-revision opening. It binds grant/revoke, the registered human target and next vault sequence; each current descriptor/body needs separate authority, and the consumer preserves witnessed rules and fully rotates items in one owner mutation. Generic precommitted administrative-rekey remains a separate context; no compatibility reader or fallback is introduced. Input-only scratch commit 802a91bccd7853562257fdb18b715ad1ce88de10 froze six inputs before runtime changes. Four conformance tests, Python consumer, deterministic generation check, Clippy and unchanged J19 gate checks passed with 41 vectors/58 protocol cases. All 39 previous vectors, 42 previous protocol cases and every other corpus section are byte/value-identical. This is fresh solo input verification, not an independent review. Runtime implementation and native validation are in progress; no QA-13 comprehensive round has started.


QA-13 input-only main commit f2cf41adf5f9aa25054304a204643af3d200483e freezes the same six exact bytes as scratch 802a91; main J19 gate passed before runtime integration. Scratch gate now binds that reachable main baseline. Runtime adds an explicit per-role owner-change request builder, shared owner/target/sequence validation, atomic all-item rotation with successor witnessed policies and fresh public labels, and a strict administrative-access plan. The real CLI driver has exercised pending, denied, partial-denied and dry-run authority with unchanged vault bytes; successful grant/revoke reached checkpoint advance/restart and exact owner reads. A revoked request was denied with a misleading malformed-artifact error; its WrongIdentity case now maps to typed access-denied request-not-authorized. Driver mistakes in sink JSON field, JSON/plaintext mode and policy ID field are corrected and not counted as product failures or passing runs. Final end-to-end run is pending. All 45 witness-engine tests (including the 16 frozen post-authentication owner-change cases), 13 protocol tests and workspace all-target Clippy pass. The initial matrix test used a serde Value decoder incompatible with borrowed-string wire IDs; it now consumes serialized JSON through the real decoder. The Clippy test-module ordering failure was repaired without suppression. No comprehensive QA-13 round has started.

QA-13 first review is frozen on HEAD 802a91bccd7853562257fdb18b715ad1ce88de10 and complete fingerprint a819b4c3c8d28a6b56ac15be6ebb2a273571aa907aceaf94c1b2a9d7914c9358. Claude restricted adapter started once. Native Codex spawn failed with agent-thread limit, so this round is single-reviewer coverage; no reviewer substitution or review claim is made. Final check-linux-owner-changes passed against the rebuilt release binaries: invalid/missing/duplicate plans, empty-vault explicit plan checks, pending/denied/partial-denied/dry-run unchanged vaults, actual atomic owner grant/revoke over two witnessed-only and one direct item, fresh epochs/seals/nonces/ciphertext digests, unchanged witness membership/operation terms, no direct slots on witnessed-only items, strict checkpoint advance/restart, exact reads and explicit revoked-owner refusal. The 21 runtime/docs/test paths are integrated into main for combined validation; prior bytes saved at /tmp/jury-qa13-integration-recovery-x5_vx7i7.

QA-13 round 1 completed with Claude alone (native Codex could not start due to the agent-thread limit), matching complete parent before/after fingerprint a819b4c3c8d28a6b56ac15be6ebb2a273571aa907aceaf94c1b2a9d7914c9358. Claude correctly found that the witness reserve/decide path bypassed the newly added owner-context validator; the live engine now uses the same manifest/policy helper as clients/approvers. A signed seven-case reserve test passes the earlier request/policy checks first, then proves invalid intents leave witness state/anchor unchanged and a valid grant reaches an actual contribution. Removing only the witness-side guard reproduces invalid owner intent was reserved; the fixed source was restored. Cancellation only has the embedded request and releases no content, so no fabricated missing-manifest check was added there. The alleged missing J19 baseline is unsupported: git cat-file resolves f2cf41a, fetched before review, and main J19 already passed; reflog/packed-ref absence is not object absence.

The overstated approval sentence is replaced by an explicit limit on what signing can enforce. Direct item bodies now open after witnessed collection. Successor policy addition uses a new batch form of the shared catalog helper so all new roots survive pruning together; required predecessor chains remain, so this does not claim constant-size history. NewItem now shares RekeyedItem cleanup-on-drop. The existing commit path checks exact on-disk vault digest, sequence, revision hash and repository ancestry under the publication lock (mutation_commit.rs:310 onward); no late N+2 reinterpretation is allowed. No resumable private request state is added. Plan/request bounds and existing label expiry remain enforced. Final live checks are being rerun.

The pre-review-repair main scripts/jig check test passed in 856.8 seconds, run-plan_sha256:7bda4e08374979028efc45a8fd09d7ca8b000ef0ce93d6f26dff0a42b9d40ea9. On the frozen first-round candidate, the real legacy Linux lifecycle also passed, all four bounded J25 ASan fuzz targets completed, alternate-provider conformance passed 27 positive/87 negative BoringSSL and 2 Argon2 cases, and cargo audit found 0 vulnerabilities and no warnings in 347 lockfile packages (RustSec commit 5a0ebedfe8bdd2e295b171f4162f8c977bcad9a5, last-updated 2026-09-02). These are not final release bindings or independent security review. Main does not yet contain the review repairs.

QA-13 review repairs passed the rebuilt real Linux owner-change driver, all 46 witness-engine tests, workspace all-target Clippy, formatting and diff checks. Main now contains all 22 runtime/docs/test paths (first-round bytes preserved at /tmp/jury-qa13-r1-repairs-recovery-n3gl46mv); its actual scripts/jig check rust-file-loc --changed-against HEAD passed. The second/final review round is frozen on scratch HEAD 802a91bccd7853562257fdb18b715ad1ce88de10 and complete fingerprint 82e33a4d3ddec88cbcc8ec4b53a9a1f221f647b87a039b3833db821a372159b4. Claude restricted adapter started once; Codex again could not start (agent-thread limit), so this is single-reviewer coverage. No findings were passed to the second reviewer. A fresh main combined test gate follows. QA-13 remains open until the final review and integrated verification finish.


QA-13 second/final review completed with Claude alone and matching complete parent fingerprint 82e33a4d3ddec88cbcc8ec4b53a9a1f221f647b87a039b3833db821a372159b4. Native Codex again could not start because of the agent-thread limit; no two-model or independent review claim. Fixed catalog batch error atomicity and exact re-add idempotency, checked active label expiry before request publication and again after collection without extending expiry, and made decrypted-content disclosure explicit in approval summaries. Added exact expiry boundary, catalog rollback/no-op, builder sequence overflow and actual JSON decoder/zero-sequence regression checks. The protocol test previously used a serde Value decoder that could fail for borrowed-string IDs before checking unknown fields; its valid round trip and negative cases now use actual serialized bytes. Existing owner-slot and automatic-operation validation make the alleged partial-direct-owner and automatic-admin publication triggers invalid loaded policies, so no compatibility bypass or fabricated failing fixture was introduced. Gate baseline f2cf41a resolves and the full unchanged J19 verifier passes in scratch; the separate CI omission was real and security CI now runs that verifier with full input history. No third comprehensive round.

The first-review-repair main combined test gate passed in 855.1 seconds (run-plan_sha256:b0d9570d4ef4251b4649e9fa2a7b9d7437c446322c6dbf196417c1b9fda8d0fb). Final second-review production repairs are integrated for fresh combined validation. Focused catalog, expiry, protocol and overflow tests passed; a Clippy test-module-ordering failure was repaired by moving the test module after production items, without suppression. Final real CLI verification is pending.

Linux packaging feasibility: two clean, offline builds of the first-review-repair candidate under pinned rust 1.97.0 Debian 12 image sha256:8fa55b2f3ddf97471ab6a767bfa3f37e6bad0986ba823e75fea57e2a2a5c3073 were byte-identical for both binaries. Highest required GLIBC symbol was 2.34; actual tested runtime baseline is Debian 12 glibc 2.36. The owner-change driver passed in that container using these binaries and real loopback daemons/PTY approvals. These version 0.1.0 probe artifacts are not final 0.0.1 release artifacts or J26 bindings.


QA-13 is complete. Final main scripts/jig check test passed in 844.4 seconds, run-plan_sha256:f603e63001a8460dc705691ae732c47c5d65dfacbf74ed8278b1b890adfcc154, including all second-review repairs. The final real Linux owner-change driver passed. The isolated release preparation checkout is /tmp/jury-linux-release-work-lo_tw9wh, baseline 1d63cc0d72b5adc7553eac9e1e1e8eecf8ce2417; its snapshot commit is local scratch only. Version 0.0.1 updates preserve dependency lock entries. Cargo +1.90.0 check --locked --workspace --all-targets passed, confirming the documented source compiler floor.

Release preparation review round 1 is frozen on complete fingerprint 41c4074d90af57746a77b4db471e6aba47f42ce0895c9b41c845ae726d8afb49. Claude restricted adapter started once; native Codex could not start (agent-thread limit), so coverage is single-reviewer. First complete local package candidate /tmp/jury-linux-candidate-r1b builds both binaries twice offline under the pinned Debian 12 image, compares exact hashes, and passes the real owner-change workflow inside that container. It packages native binaries, source, vendored dependencies, CycloneDX inventories, license notices, provenance and checksums. Source/artifact verification passes, and rejects the earlier real candidate after the script changed. Signing identity/private reporting contact remain unanswered; no signing or publication is performed. Review adjudication and remaining artifact QA are pending.


The r1b 0.0.1 native archive passes SHA256SUMS verification, actual extraction, install -Dm755 into a fresh bin directory, exact version output and help for jury/juryd. Both ELF files require no GLIBC symbol newer than 2.34; the tested container baseline remains Debian 12 glibc 2.36, not an untested 2.34 support claim. The source archive preserves internal license symlinks and excludes workflow state/marketing files; SBOM references contain no temporary build paths. Against an isolated copy of these real artifacts, one-byte SBOM modification gives artifact-drift refusal and an extra file gives inventory-drift refusal. An optional signature bundle is permitted but explicitly not authenticated by the local consistency checker. No signing result is fabricated.

All existing native CLI drivers passed against the binaries installed from r1b: role onboarding (including separate remote identities, strict authenticated import/update/rollback refusal and three-role recovery), descriptor access, approval PTYs, direct/witnessed lifecycle, shared-pair and disjoint-pair global checkpoints with third-policy/new-witness activation, and diagnostics/timeouts. The package builder itself already passed the real owner-change workflow in Debian 12. Cargo +1.90.0 all-workspace/all-target compilation passed. These are the first frozen release-preparation candidate's results; review completion and final main integration remain pending.


Release preparation round 1 completed with Claude only and matching complete fingerprint 41c4074d90af57746a77b4db471e6aba47f42ce0895c9b41c845ae726d8afb49. Repairs remove the eight-CPU Docker quota, execute version checks in the pinned image, make QA binary mounts read-only, retain the compared binary hashes and verify them inside the final tarball, explicitly label dirty unsigned candidates, and select operating/security docs for the native package. The historical audit version wording is corrected. SBOM generation now uses cargo-cyclonedx 0.5.9 compiled and run inside the pinned image with its Cargo/Rust resolver, checks the disposable SBOM source and archived source against the original inventory, and preserves the lock. Vendor reuse accepts only a hash-verified archive with exact revision mappings derived from Cargo.lock. A current cargo-audit 0.22.2 report is packaged, with advisory filters/ignores refused and warnings treated as failures.

Broader license matching exposed ten locked package archives without a matching license file. Verbatim upstream supplements are recorded and hash-bound under licenses/vendor-supplements. Nine use crate VCS metadata; the Android delivery archive has no VCS record, so its supplement comes from the exact consuming rustls-platform-verifier 0.7.0 revision without claiming that revision produced the Android archive. r-efi includes the full MIT terms and attribution in AUTHORS. All 339 vendored packages now contribute license/notice material. Five real-filesystem regression tests cover archive authentication/traversal, source drift/modes/symlinks, locked provider mapping, missing/unconventionally named notices and post-packaging binary hashes; security CI runs them. These filesystem fixtures are not represented as live CLI proof. The complete r2 package build passed and has the same exact jury/juryd hashes as r1b, so the previously completed packaged-binary Linux journeys still apply.

Second/final packaging review is frozen on complete fingerprint 6ec1834672097e7acf277a0ce9bb525b2e1a006a64192ef3ad3f1b7f0ad634fb. Claude restricted adapter started once; native Codex could not start (agent-thread limit). No third round. Fresh J19 verification passed. All four bounded J25 ASan fuzz targets passed at the script's 10-second per-target budgets; the fuzz workspace remains separately locked and no assertions or budgets changed. Alternate-provider conformance passed 27 positive/87 negative BoringSSL and 2 Argon2 cases. The bounded repository leak scan used the exact installed release binary by changing only the harness's binary locator (runpy function globals); all real CLI operations/oracles remained intact. It found zero exact hits in named surfaces and detected its deliberately seeded leak. Final main integration, measured resource matrix, required work gates and a main-bound package remain pending.

The refreshed product dependency audit reports zero vulnerabilities and zero warnings in 347 packages (RustSec commit 5a0ebedfe8bdd2e295b171f4162f8c977bcad9a5, last updated 2026-09-02). The pinned SBOM tool's own installation warns about its locked xml-rs 0.8.19 being yanked; this is recorded separately from the product lock audit, not suppressed or represented as a product dependency. Read-only GitHub API verification confirms private vulnerability reporting is disabled. The private-reporting contact and signing identity question remains unanswered; no repository setting or signing identity was invented or changed.


Final packaging review completed with Claude only and matching complete parent fingerprint 6ec1834672097e7acf277a0ce9bb525b2e1a006a64192ef3ad3f1b7f0ad634fb. The reviewer reported an evidence omission for scripts/test-linux-release.py but read it directly; its coverage limitation is retained, despite the complete source fingerprint. Native Codex could not start; no third round. Accepted repairs use exact notice names and require root/declared/supplement terms (all 339 actual vendor packages pass), record notice paths, validate/normalize expected SHA-256 and exact hex Git revisions, restrict SBOM rewriting to reference declarations/edges, require an external checksum expectation in the CLI verifier, and offer exact expected binary hashes for reproduction. The verifier explicitly warns that any Sigstore bundle remains unauthenticated. The build varies job counts 8/4 and documents its pinned-environment scope rather than claiming arbitrary-environment reproduction. Source dirtiness excludes the same workflow/site paths as the source inventory. Failed builds remove their staging output; only successful verification publishes the candidate directory. Python floor is 3.11.4 and actual compiler/Cargo/OS/glibc observations are recorded and checked. Eight filesystem regressions pass, including real archive/verification tests, and all 339 vendored packages pass the tightened notice check. The untracked-script warning is addressed by integrating all 30 packaging paths together, with prior main bytes at /tmp/jury-release-integration-recovery-jb3cmhql; no application commit has been made. Public license attributions are preserved verbatim.

The final main source now reports 0.0.1. Required Jig gates, the 46-case measured resource matrix and a fresh main-bound package follow on an immutable source tree. The new build will compare both binaries against the already-tested candidate hashes a9d6c79f8ab212c0073daf1919d7f0e8207b9350cfa182138819ffb2316d72c2 (jury) and eca86bf765e166e7382a674423ff076ea1e441b22ccb1c02c6ae3b29b799e9e0 (juryd), so prior actual CLI journeys apply only if exact equality succeeds. Publication still awaits the private reporting contact and signing identity; no identity, review certification, signature, tag or publication is fabricated.


The main-bound 0.0.1 package completed at target/linux-release/0.0.1-candidate. Two offline builds (8/4 jobs) match each other and the already-tested native hashes. The Debian 12 owner-change driver passed. Real package extraction/version checks, all checksums, all 339 notice paths and source bindings pass; a coordinated artifact/provenance/checksum rewrite fails against the original expected checksum digest. SHA256SUMS digest is 6bad47a8932e776c08e79317f7f9e8f75df61ddafb886ef1243307454c625bfc. Provenance records actual Rust/Cargo 1.97.0, Debian 12/glibc 2.36, source HEAD f2cf41a plus exact dirty source inventory, and signed=false/published=false. The 46-case main J25 resource matrix passed and its source digest matches; maximum process RSS among the 46 cases was 533052 KiB for backup-kdf-hardened (includes fixture setup; concurrent build/test activity means these single observations are not an SLO). Timing smoke statistics remained below their unchanged gross-divergence threshold; no constant-time proof is claimed.

The configured work check exposed two harness facts. First, the full 0.0.1 workspace test passed in 875.7 seconds (receipt_01M1VG90RG6EJX7WCEDB7RE3C0), with formatting, Clippy and contract passing. Second, its original-baseline Rust LOC check failed on conformance/witness-v1/src/lib.rs at 3178 lines; prior HEAD-scoped checks missed that committed-input file. This was an incomplete scope check, not a valid full-gate pass. The work configuration then ran the same full suite again for its overlapping verify profile. That duplicate was cancelled with SIGINT after 605.7 seconds, terminal exit 130; its incomplete test result is not counted. All earlier completed receipts remain intact. No gate limit, expected output, test assertion or selector policy was relaxed to force green.

A scoped conformance layout repair is isolated on scratch HEAD 369f287b38e876cac0c78dd2b1243b6996366843. It splits generation, cases and consumers into files below 1000 lines, with a RequestFixture moving the same values between request and contribution/receipt generation. All four existing tests, Clippy, deterministic generation and the alternate consumer pass against the byte-identical existing corpus; no vectors/protocol semantics changed and no Jury runtime code changed. Input-only side commit e54bc736e9972a27c3568f3a0ad6b5e1b0d40793 has parent f2cf41a. The gate adds four exact source bindings (19 total) and verifies the actual Rust-source inventory; a temporary extra unbound source is refused before the checks and was removed. The first local rebind attempt correctly failed on the separately bound AGENTS guide; the completed side commit includes it and the full J19 gate passes. This is a source-layout rebind, not retroactive construction approval.

The focused review is frozen on complete fingerprint 9ca9c7a8f767deb042e625b3c06e5034be2fba3f91648e225513ddb2759340a5; Claude started once, native Codex again could not start because of the thread limit. Final review, main integration and renewed artifact binding are pending. The main package above is valid for the pre-layout main source and must not be called the final source binding after this repair integrates. Future verification will select the necessary gates explicitly to avoid the duplicate-suite path; the configured gate requirements themselves remain unchanged.


The layout repair expanded after complete candidate checking exposed seven additional runtime files above the unchanged 800-line limit. The original changed-against selector omitted uncommitted/untracked files; its earlier passes do not establish complete candidate compliance. Methods, helpers and existing tests are now split into child modules while retaining effective visibility and assertions. Actual temporary-index staged LOC checking includes the entire candidate without staging the user's real index. Scratch workspace all-target Clippy and all 139 active core tests pass (one existing measurement test ignored).

Layout review round 1 completed with Claude only and matching fingerprint 9ca9c7a8f767deb042e625b3c06e5034be2fba3f91648e225513ddb2759340a5. Repairs bring vectors.rs below 800 lines and compare bound, on-disk and pinned-revision Rust inventories, including Cargo automatic build/test/example/bench sources. Four actual temporary-Git regression tests pass. The unchanged corpus retains SHA-256 d850a1543e6d7868b221d2f4ab51e561fac1565143df4c065217cfb18520343a. Reachable input commit 6b467d67c8f3d991c8256a8d4a90c8b82831e614 and control commit ea494154132dc3e10c6b1798d6c17114f2495f92 replace the earlier dangling side revision. Full J19 verification passes from a clean clone of their branch. Main fast-forwarded through only these input/control commits; all application work remains uncommitted. The exact 27-path layout change is integrated, with recovery bytes at /tmp/jury-layout-integration-recovery-8esujhmi.

Second/final layout review is pending on scratch HEAD 369f287b38e876cac0c78dd2b1243b6996366843 and complete fingerprint 5ce974acc6a090f8f9d813cfe1e21d3802ec83de31e286eaeeb64387ee91fb66. Claude started once; native Codex could not start because of the thread limit. No third round. Fresh main validation and package binding follow; prior package and measurement records apply to the preceding source only. The configured verify profile is selected once to avoid running the overlapping legacy/profile test suite twice; no gate configuration is changed.


Final layout review completed with Claude alone and matching complete parent fingerprint 5ce974acc6a090f8f9d813cfe1e21d3802ec83de31e286eaeeb64387ee91fb66. The reviewer inspected all new files but only part of the moved mutation tests; the parent compared the entire old module after rustfmt and it matches exactly (one original line-wrap changes). Native Codex could not start; neither dual-model nor complete provider coverage is claimed. No third round. The revision-availability concern is addressed by main HEAD ea49415 containing 6b467 as an ancestor and the clean-clone J19 pass. Pushing is a publication step and has not occurred. The additional verifier self-attestation concern is calibrated against the documented J19/J26 split: J19 freezes construction inputs; J26 binds the exact verifier, tests and all other source. An actual extracted-source verification passes unchanged, then rejects simultaneous checker-byte and TOML checker-hash changes against the final release binding. This does not claim protection against a committer replacing both an entire release and its externally authenticated expectation. The sibling direct conformance gate lacks the new witness inventory rule, a pre-existing developer-gate coverage limitation; the complete J26 source inventory includes that crate and rejects source additions/drift. No new direct-conformance bypass was introduced by this layout change. Target-tree traversal and the private environment module name are low-priority efficiency/naming observations without a reproduced runtime failure; they do not justify another gate rebind. No medium-or-higher introduced release defect remains validated.

Final main verify profile passed all five targets: Clippy, formatting, full workspace tests (885.8 seconds), contract and original-baseline LOC. Run-plan SHA-256 e1358ff34460e7d6672ebc6d58b464ccb09508e9b38c9d99fec1276ba3f6964b; run_01M1VJTTDWZQ1QNJDQP35H3VB1. The separate complete-candidate temporary-index LOC check also passed and the real index remains unstaged. Main J19, all four gate-regression tests, all eight packaging regressions, Rust 1.90 all-workspace/all-target compilation, all four bounded ASan fuzz targets and five fuzz-workspace tests passed. Alternate-provider checks passed 27 positive/87 negative BoringSSL and two Argon2 cases. All 46 measurement cases exited zero; source digest 7a29da1dfd312f34a5c2dfc4c37ff2480ad22459d94219f7319648a21658516b, maximum observed RSS 534048 KiB for backup-kdf-hardened. These concurrent single-machine measurements are not SLO or constant-time proofs.

The final main-bound local package is target/linux-release/0.0.1-candidate-layout, source HEAD ea494154132dc3e10c6b1798d6c17114f2495f92 plus exact dirty source inventory. SHA256SUMS digest ef97235639246648d1b6ba291223e43318bcf4aece1866e7808960bb2f1e71bb. Two empty offline builds at 8/4 jobs match: jury cdaafaebf6740c6039a05b5f56759b76dfb8c70a6b12c801f90b3c4da75779d1; juryd 347d2d61799d14bb0c91267156324d23f2ad3fb2652b981c588c5ada32784199. Because layout changed these hashes, every real Linux driver was rerun against these installed binaries: onboarding/remote import/update/rollback and three-role backup/restore, descriptor/automatic field access, approval PTYs, shared and disjoint policy checkpoints with new witnesses, lifecycle/restart/replay, and diagnostics/timeout enforcement. All passed. The package builder also passed owner grant/revoke in Debian 12. Actual native archive extraction, version/help, all seven checksums and equality to the exercised installed binaries passed. The exact installed binary passed the bounded repository leak scan: zero exact hits for two needles across the named surfaces, seeded leak detected; explicit exclusions retained. The product audit has zero vulnerabilities/warnings in 347 packages at RustSec 5a0ebedfe8bdd2e295b171f4162f8c977bcad9a5. The SBOM tool's own yanked xml-rs warning remains recorded separately. No signing, publication or independent security review is claimed.

Jig work gates/evidence recognize the fresh successful verify profile but do not reuse its normalized tool receipts for the five duplicate legacy gates. Those still select pre-layout gate-scoped receipts. Selecting --gate verify itself is unsupported by this runtime (unknown configured check gate), so the profile was run through jig check --profile verify. To satisfy the unchanged repository completion rule, the five legacy gates will now run explicitly once. This repeats the already-passing test suite for receipt compatibility only; it is not new product evidence or a reason to weaken the gates.


Final legacy work check passed all five configured gates on unchanged source. Full tests passed again in 887.6 seconds, receipt_01M1VMMZ7WSDFF54T1455RVZZP; batch receipt_01M1VMN5TADVA3RWN7ER2C5RXF. This second successful run satisfies the legacy receipt format, not an independent assurance claim. All requested QA findings and their local candidate verification are complete. The separate J26 publication task remains in progress: the private reporting contact and signing identity have not been supplied, GitHub private vulnerability reporting is disabled, and no signing/tagging/pushing/publication was performed. The final source archive intentionally retains the audit chronology; this excluded work plan records the final results without changing the verified source or rebuilding an identical binary merely to update a status paragraph.
