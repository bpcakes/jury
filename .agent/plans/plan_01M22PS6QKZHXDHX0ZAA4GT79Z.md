Consumer: Linux release maintainer. Feature gated: publication of the exact experimental witnessed-access candidate. Observed defects: the fuzz workspace lock omitted a dependency edge, and previous candidate evidence predates the final source. Delete this recovery plan when this candidate is superseded or no longer needs recovery.

Local preparation scope: repair the lock without relaxing locked checks; permit existing integration assertions to execute explicit candidate binaries; add the missing real TLS lifecycle check; build a clean-source candidate twice; verify its artifacts and run the packaged journeys. J26 publication, signing identity, private reporting, and release approval remain outside this completed-local-preparation boundary and keep J26 open.

Source: 7b246600086b52e47a29474fcef8464764701019, committed and pushed. The fuzz lock changes one dependency edge and no package versions. Candidate overrides fail for explicitly missing binaries; normal Cargo selection remains the default. TLS fixtures generate a synthetic CA and signed service certificate, use HTTPS with explicit trust on both links, and require wrong-CA transport refusal without output or a success receipt before the successful lifecycle.

Candidate: target/linux-release/0.0.1-7b24660. Clean source; unsigned and unpublished. Both pinned Debian 12 builds reproduce jury 63f0a4203e73fd7ea3a0a8a6358980a8f042d13956f4e2473255c811b862f5ae and juryd c0a065fad8a792d09aa677d216acc101e202fd9b944f7d9c898df5510b42c2ef. SHA256SUMS digest: 37d591a928aa337350b6e5c24a32260ce1beb05636aa4371d6589d508afb424c. Extracted binary hashes match. Application and SBOM-tool advisory reports both contain zero vulnerabilities and zero warnings. Provenance binds source, verifier, providers, build inputs, binary hashes, and archive inventory; local hash verification is not publisher authentication.

Passed: original four-target bounded fuzz smoke; fresh J19 and rollover inputs; suite-2 provider and cross-provider primitive/context conformance; J25 46-case measurement matrix; scoped repository leak scan with zero exact hits and successful seeded-leak detection. Measurement report source digest 0d11b078d659b47ca6360f42f098795dd086b929c5fb988e880f40200ba52424 matches the committed source bytes, although its recorded Git HEAD is the precommit baseline. These source-level checks do not claim to execute the packaged binary unless stated.

All six required Jig gates passed with fresh evidence (batch receipt_01M22QN76MV8WNAT8EDQGFB8K7). The final scripts/jig check test passed with optimization level 1 and debug assertions and overflow checks enabled. Earlier in-flight work-check evidence was invalidated by source edits; the clean-source rerun supersedes it.

Exact-artifact checks passed: 26 native CLI integration tests, including direct/governed rollover and suite migration; three self-hosted tests using real packaged juryd with TLS, SQLite, anchor/restart/shutdown assertions; 93 public help surfaces. Native CLI quorum fixtures use embedded engines and are not packaged-daemon durability evidence. The separate self-hosted tests cover real daemon persistence. All 21 release-helper tests passed against the newly built container juryd:j26-7b24660 with its exact source revision.

The Debian 12 unprivileged, network-isolated run passed all ten packaged journeys plus the TLS lifecycle, including untrusted-CA refusal. The source and complete artifact inventory were reverified afterward against the checksum digest above. Local preparation is complete; J26 stays in progress for private reporting readiness, publisher/signing identity, remaining CI and publication approval. Logs: target/j26-0.0.1/build.log, packaged-journeys.log, packaged-integration.log, container-build.log, work-check.log, final-test.log. Build output and logs are local ignored files and may need regeneration. CI Security invariants, maintained provider, Repo Policy and Agent Map checks pass on 7b24660; Rust Tests remains in progress. Nothing here is independent security review or a claim of protection for real secrets.
