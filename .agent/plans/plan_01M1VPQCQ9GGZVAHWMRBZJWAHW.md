Consumer: maintainer deciding Linux 0.0.1 release readiness, explicitly requested by the user. Gate: native CLI and juryd functional/UX readiness. Observed defect class: previous QA exposed broken onboarding and input handling; rerun current candidate and probe new user paths. Retirement: supersede on candidate changes or completed release decision. No product capability credit for this audit. Preserve existing uncommitted work. Verify package/source consistency, fresh release build, installation/help, direct and witnessed journeys including PTYs, recovery and process behavior, then repository checks. Report concrete failures and untested scope; do not sign, publish, or close J26.
Audit progress (2026-09-06): eight packaged Debian 12 CLI/daemon/PTTY journeys passed, plus a real HTTPS lifecycle with explicit synthetic CA. Fresh exploratory CLI probes found QA-14 (ambiguous dotted references) and QA-15 (ignored environment passphrase), both reproduced against the package and fresh source release build. QA-16 covers misleading help. Findings are recorded in docs/qa-linux-0.0.1.md and local tasks jury-qv4.3.12, jury-qv4.3.13, jury-qv4.3.11. The first two block J26, which remains in progress. No application/test/gate edits, commits, signing or publication.

Process worksheet: runtime does not branch on this report, so it is process. Consumer/gate/observed defects/retirement are named above and the user explicitly requested this audit. Integrity exception is not invoked (all four exception questions are inapplicable because the report passes the ordinary creation gate). Highest priority release capability is J26; the new positive repair work is QA-14. The requested audit, including failed and successful real journeys, is more useful for this session than inventing an unrequested feature. Verdict: legitimate minimal release QA report, zero product-capability credit.

Real-work worksheet: session produced no commits or closed product items; classification tally for that denominator is zero in all classes. The report and tracker changes are process, explicitly requested for the release decision. No feature was shipped; a two-minute demo can reproduce a template selecting the other field. Without the report the maintainer would miss that observed defect. No speculative enabler was added. J26 remains the active user-visible release task; this QA serves it. Solo run: no swarm comparison applies. No original acceptance condition was edited or moved to close J26. Verdict: bounded requested audit; implementation remains open.

Honesty inventory, bounded to this audit session:
1. No (checked commands and changes): no repository tests weakened/deleted/ignored; no commits or history rewrites.
2. No (checked scratch drivers): synthetic inputs launch real CLI/daemon processes; no mock supplies a successful product response.
3. No (checked changed paths): no golden/snapshot regeneration.
4. No (checked changed paths): no implementation, validator, CI, suppression or tolerance changes. Scratch probes had mistaken expected exit codes and a faulty terminal-output parser; these were disclosed in the report and corrected using the actual contract/output, not by changing the product.
5. No (checked release conclusion): reproduced failures hold release despite passing standard journeys.
6. No (checked suite outputs): only populated suites/journeys are counted; zero-test generator/doc targets are not evidence of behavior.
7. No (checked tool transcript against report): running workspace/gate checks are explicitly pending until observed complete.
8. No (checked claims): local synthetic services and self-verification are not independent review or multi-host proof.
9. No (checked report ordering): release hold and new failures lead; prior success is qualified.
10. No (checked evidence-bearing commands): stderr is returned, captured with stdout in retained logs, or inspected after safe capture. Secret/certificate-generation material is never presented as output evidence.
11. No (checked br show): J26 remains open and is blocked by the repairs; no product task closed.
12. No (checked report and source): no requirements changed to fit the product.
13–17. Not applicable: no subagents or delegated work in this session.
18. No (checked metric wording): denominator is the eight preselected packaged journeys; failures and untested surfaces are reported separately. Exploratory case counts are not used as a coverage score.
19. Explainable mistakes: unsupported audit/Jig/tracker arguments were corrected, early scratch expected exit codes were guesses, and the first terminal-mode probe compared escape sequences. None is claimed as successful product verification. Standalone test and mandatory work-check execution overlap; they count as one engineering evidence class, not independent confirmation.
20. Strongest evidence: the same packaged binary that passes exact standalone reads demonstrably resolves a colliding template reference to the other field; the scratch reproducer and self-contained tracker reproduction can be rerun. The release verdict remains hold.
Older-session review is outside this functional audit; prior claims were treated as historical and relevant runtime journeys were freshly executed. Disposition: no repository test/gate weakening or false completion found in this session; harness mistakes disclosed rather than counted as product failures. Avoid proof-class inflation and follow-up laundering: no release closure or security certification.

Verification sequencing correction: the first Cargo workspace suite completed with exit 0 and no failing tests, but Jig rejected the record because audit report/tracker edits changed the worktree during a declared read-only check. The concurrent work-check started from the same earlier snapshot and cannot provide fresh final evidence. Freeze all tracked report/tracker/plan edits before final work check. Retain the initial failed record and disclose this orchestration mistake; do not relax the effect policy or count the failed Jig receipt as passing. Final check log: target/qa-linux-0.0.1-do73vlcw/final-work-gates.log. This is a correction to the earlier pending-check note, not a retraction of any claimed passing Jig test gate (none was claimed).

Final discovery update before verification freeze: following QA-16's terminal input guidance exposed QA-17, a real PTY field-value echo with successful exact storage. Verified first with inherited identity passphrase, then with an actual hidden passphrase prompt. New task jury-qv4.3.14 blocks J26. No runtime changes. The obsolete verification run was cancelled before recording this additional finding.

Honesty correction to answers 7/9/19: the initial QA-17 tracker description prematurely claimed the second normal-prompt variant before that probe completed. That first variant-driver timed out on case-sensitive prompt matching. The tracker note immediately corrected the claim, then a separately observed corrected driver reproduced the issue. The report discloses this chain. Countermeasure: do not combine a dependent claim-writing action with retrieval of uninspected probe results; a planned probe is not proof (proof-class inflation). Production code/tests/gates remain unchanged.

All discovery, report, tracker and plan edits are now complete. No more tracked edits while final-work-gates.log is being produced. All 91 advertised help surfaces also rendered successfully. Final audit result is HOLD: QA-14, QA-15 and QA-17 block release, QA-16 is minor UX, signing/private reporting remain pending. Audit completion is separate from release completion.
